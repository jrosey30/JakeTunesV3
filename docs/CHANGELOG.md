# JakeTunes Changelog — 4.0.5 through 4.4.37

> **Purpose**: bring any contributor (or another Claude session) up to speed on everything that's shipped since 4.0.5. Grouped by minor version with a header summarizing the *theme* of each line, then each patch with a one-paragraph "what & why."
>
> Reading order: 4.0.x (foundation hardening) → 4.1.x (playback resilience + Radio Mode v1) → 4.2.x (Radio Mode v2 — cast, mid-track-crash hunt, picks expansion, recording) → 4.3.x (external APIs + memory + station ID production) → 4.4.x (show bible, caller rolodex, hour clock, polish & regressions).

---

## 4.0 line — foundation hardening

The 4.0 series was about getting playback / sync / metadata bulletproof before any "fun" features. The first half is sync-engine fixes, the second half is the playback-pipeline instrumentation work that exposed and fixed the resource fights between Airfoil, audio analysis, and the renderer.

| Version | Theme | Notes |
|---|---|---|
| **4.0.5** | Sync: dropped the 5-min full-library tag-verification preflight. The preflight blocked sync for 5 minutes scanning files for no reason; removed entirely. |
| **4.0.6** | Airfoil / external-pause auto-recovery: detect when Core Audio renegotiates output device under us and resume playback. |
| **4.0.7** | Queue fixes: shuffle reorders the actual queue, sequential playback, drag-reorder works. |
| **4.0.8** | Audio-analysis retry logic + atomic override writes (no torn JSON on crash). |
| **4.0.9** | Diagnostic ring buffer (`__audioLog()`) on the audio pipeline. Captures the last 50 events for post-mortem of "music stopped playing" issues. |
| **4.0.10** | Playback wins resource fights. Background workers (audio analysis, ALAC prewarm) yield while music is playing. |
| **4.0.11** | Throttle `SET_POSITION` dispatch to 10Hz. Was firing 60Hz, saturating the renderer thread, causing audio decoder to starve during mouse-move/scroll — the "broken record" stutter. |
| **4.0.12** | Reverted the auto-resume on `onpause`. It was creating a feedback loop where buffer-underrun → pause → auto-resume → re-buffer → pause again. Logging stays; auto-resume gone. |

---

## 4.1 line — playback cache + Radio Mode v1

4.1 introduces the play cache (ALAC → AAC pre-transcode so first-play isn't a 3-second hang) and ships the first version of Radio Mode (between-track WJLR-style commentary). Most of the line is bug-bashing around the cache, metadata-write races, and one big new feature ship.

| Version | Theme | Notes |
|---|---|---|
| **4.1.0** | **Playback cache** — three-part rework. ALAC→AAC transcode cache (~10 GB for typical library) so ALAC tracks start instantly. |
| **4.1.1** | Serialize metadata-overrides writes. Concurrent saves were stomping on each other; analysis data was being silently wiped. Single-flight writer. |
| **4.1.2** | Fix position-bar freeze after scrub-ahead. |
| **4.1.3** | Version display sourced from `package.json` (was hardcoded). Visible in Device view. |
| **4.1.4** | macOS Desktop permission crash + EPIPE harden across all `spawn`-and-write sites. Sequoia tightened sandbox permissions; we were crashing on import. |
| **4.1.5** | Stuck-audio watchdog. Airfoil's 10-min device-renegotiation hijack was leaving the Howl in a frozen state — recreate from scratch and resume from last known position. (Later disabled in 4.2.12 — see note.) |
| **4.1.6** | **Radio Mode (WJLR 330.9)**: between-track commentary, two voices (Music Man + Megan as bickering co-hosts), no delay between songs and chat. |
| **4.1.7** | Megan as co-host. Two-voice show; reactive dialog instead of solo monologue. |
| **4.1.8** | Radio Mode defaults to whole-library shuffle; right-click → "Start Artist Radio." |

---

## 4.2 line — Radio Mode v2 + cast expansion + the 29-second crash

The longest, messiest line. Radio Mode v2 (full cast, station IDs, broadcast feel), the multi-week hunt for the 29-second mid-track audio cutoff, the picks system (Music Man Picks → 3 weekly picks), recording → MP3, and the human-talk overhaul.

| Version | Theme | Notes |
|---|---|---|
| **4.2.0** | Visual polish on Radio Mode + DJ Mode vinyl-icon placeholder. |
| **4.2.1** | **Announcer voice** for campy WJLR station IDs. Distinct from MM/Megan. Hide bubble during Radio Mode. |
| **4.2.2** | Show opener (campy ID on Radio toggle on), profanity allowed, broadcast-wave animation, two-button toolbar layout. |
| **4.2.3** | Radio voices stop sounding scripted. Switched to `eleven_turbo_v2_5` model + reactive prompt + style 0.7. |
| **4.2.4** | (Skipped/internal.) |
| **4.2.5** | Switch default host between Music Man and Megan in Preferences → AI. |
| **4.2.6** | Wire the show opener — clicking Radio now plays the WJLR drop FIRST, before the first track. |
| **4.2.7** | Deterministic announcer scheduling. Opener + every 4th transition gets an announcer drop. Replaces the prior "Claude rolls a die" approach. |
| **4.2.8** | Mic mode back in top toolbar. Sidebar's orange button → vinyl DJ Mode. |
| **4.2.9** | 5-sec debounce on audio-analysis worker — librosa was running concurrently with playback, starving the audio decoder. (Worker later disabled entirely in 4.2.12.) |
| **4.2.10** | Megan voice ID swap (`T7eLpgAAhoXHlrNajG8v`) + phonetic announcer drops ("double-yoo... jay..."). |
| **4.2.11** | **29-second mid-track audio cutoff hunt, attempt 1**: replaced `Buffer.alloc` + `fh.read` (whole-file-in-memory) with streaming response via `createReadStream` + `Readable.toWeb` in the `ipod-audio://` protocol handler. Theory was Buffer GC; theory was wrong. |
| **4.2.12** | **29-second crash hunt, attempt 2**: disabled librosa audio-analysis worker AND the stuck-audio watchdog from 4.1.5. Both were suspects. Watchdog had false positives; librosa raced with playback. Both stay off. |
| **4.2.13** | **29-second crash hunt, attempt 3**: `powerSaveBlocker('prevent-app-suspension')` to defeat macOS App Nap. Plus heartbeat diagnostic (every 2s logs full audio state). Plus confident-announcer prompt rewrite (no more "double-yoo..." ellipses). |
| **4.2.14** | Heartbeat becomes an active recovery loop. If position is frozen >4s and Howler thinks it's playing → kick `node.play()`. >8s frozen → hard recover (recreate Howl + seek back). |
| **4.2.15** | **Real Radio Mode banter fix**: `handleRadioToggle` was passing `djTransition=false` to `playTrack`, dispatching `musicman-dj-cancel`, which killed the module-level `autoDjMode` flag *permanently* for the session. Result: opener fired, then no banter between tracks. Fixed by passing `djTransition=true`. |
| **4.2.16** | **Duck music under banter** (real-radio-DJ style). Next track starts ducked at ~15%, banter plays over the top, music fades back up on segment end. *Later revised in 4.3.2 — see below.* |
| **4.2.17** | **Topic rotation**: 18 conversation angles randomly picked per segment (back-announce, lateral pivot, lyric roast, Brooklyn local color, MM historian, …). Plus mandatory opener line: "Here's Megan, and the one, the only, the MUSIC MAN!" |
| **4.2.18** | **Megan Picks** added alongside MM Picks. Both 25 tracks, **weekly Friday-to-Friday rotation** (was daily). New default MM voice ID. |
| **4.2.19** | **Cast expansion**: **Giovanni** (recurring caller — Bay Ridge, earnest, asks music questions) and **DJ Hands** (rare guest, in-house DJ, party-first). Rotation slot map per 12 transitions: slot 0/4 = station ID, 5/11 = Giovanni, 9 = DJ Hands, else = MM+Megan. |
| **4.2.20** | **Record radio show → MP3** (Record button in toolbar, MediaRecorder → ffmpeg → save dialog). **Human-talk overhaul** (kill vanilla / kill Wikipedia recital — hard prompt guardrails). **DJ Hands → party-first** (rewrote `DJ_HANDS_CORE` after his picks came back as a beat-criticism syllabus). |

---

## 4.3 line — external APIs + memory + station ID production

Six external integrations, persistent show memory, broadcast-FX chain for the announcer voice, and the v3 TTS switch.

| Version | Theme | Notes |
|---|---|---|
| **4.3.0** | **Six external API integrations**: OpenWeatherMap (Brooklyn weather), Last.fm (charts + similar artists), Pitchfork/Stereogum/Quietus RSS, Discogs (release detail), Wikidata (artist info), Cover Art Archive. All cached, all fail-soft. **DJ Hands renamed to DJ Stephen Hands.** Harder artist-mix rule (max 1 per artist in picks, never 3+). |
| **4.3.1** | **ElevenLabs v3 TTS** for non-fast paths. Plus inline performance markers ([laughs], [scoffs], [sighs], [whispers], [excited], [interrupts]) in the radio prompt — v3 performs them as actual sound. |
| **4.3.2** | **Real-radio timing fix**: dialog plays in silence at the seam, next song fades up after the last segment. The 4.2.16 ducked-overlap approach was leaving the next song playing 30+ seconds at 15% before banter actually dropped. Also: prefetch window pushed from 30s → 60s remaining. **Persistent show memory** added (`src/main/radio-memory.ts`): last 8 angles + last 8 callback-worthy lines + slot-1 hot take + running-bit state, all in `userData/radio-memory.json`. |
| **4.3.3** | **Station ID production**: broadcast FX chain on the announcer voice (compressor 8:1 / low shelf +4 / presence peak +5 / high shelf +2 / convolver reverb) — sounds like a real FM imaging cut, not synthesized speech. Plus **procedural stingers** (Web Audio): riser, swoosh, drum hit, bell hit, sub drop, whoosh pad. Stinger before announcer voice, drum hit after. |
| **4.3.4** | **No-station-ID bug fix**: v3 TTS was silently failing for the announcer voice; the synth loop dropped the failed segment from the array, opener played MM/Megan but no announcer. Added per-call model fallback chain (`eleven_v3` → `eleven_turbo_v2_5`), hardened announcer FX bind to fall back to plain routing on failure, comprehensive logging across the radio path. |

---

## 4.4 line — the show bible (caller rolodex + hour clock) + recent polish/regressions

A formal "Show Bible" was generated by claude.ai (full structural framework — hour clock, 11 archetypes, 9-person caller rolodex, defer/counter ratios, running bits, energy/dwell tags). 4.4.x is the implementation, in phases.

| Version | Theme | Notes |
|---|---|---|
| **4.4.0** | **Phase 1 — Caller rolodex.** 8 new callers beyond Giovanni: Rajiv (format antagonist, challenges premises), Bernard (elder statesman who was actually there), LaShonte (contemporary corrective), Kristina (metal purist), Devin (wrong-show comic relief), Maya (real-question-asker), Mike (industry insider), Zoe (wildcard take-haver). Each in `src/main/cast.ts` with voice ID, weight, function, background, speech profile, MM/Megan reactions, never-list, voice settings. Scheduler: weighted random + no-repeat-within-3 + Giovanni-only for first 2 caller slots of session. **DJ Mode is now Stephen Hands end-to-end** (set generation, set intro, between-track commentary — all via `DJ_HANDS_CORE` + `DJ_HANDS_VOICE_ID`). **Picks featured section** in sidebar (`WJLR PICKS` group, distinct visual treatment from standard smart playlists). |
| **4.4.1** | **Phase 2 — Hour clock + 11 archetypes.** `src/main/archetypes.ts` defines 11 structural templates (Cold Open Hot Take, Lateral Pivot, Lightning Round, Deferred Punchline, Lineage Bridge, Lyric Roast, Brooklyn Texture, Historian Dwell, Hour Out, Back-Announce, Recovery). Slot-by-slot routing: slot 0 = full ID, 1 = Cold Open Hot Take (captured), 2 = back-announce, 3 = lateral/lineage, 4 = brooklyn/lyric-roast/lightning, 5 = caller, 6 = recovery, 7 = mini station ID (new — short dry mid-hour), 8 = Historian Dwell (slot 8 only — MM monologue), 9 = Stephen guest 1-in-3 hours / Lightning Round otherwise, 10 = recovery, 11 = Hour Out (pulls slot-1 hot take from memory + pays it off). |
| **4.4.2** | **Lock 3 picks personas into distinct lanes.** Music Man = classic rock canon + art rock + heritage jazz + singer-songwriter heritage. Megan = contemporary indie + critic territory + sharp left-field + underrated singer-songwriter. Stephen Hands = dance/disco/boogie/house/techno/hip-hop/electronic. **5-tier library-aware fallback** for Stephen so his picks never come back empty even if the library lacks pure dance music. **Regenerate button** added to picks panels (force fresh pull, bypasses weekly cache). |
| **4.4.3** | **90-minute Radio Mode cap.** Auto-stop after 90 min broadcast time. Visible elapsed/remaining countdown in the ON AIR pill. |
| **4.4.4** | **Caller knowledge boundary.** Hard rule in prompt: callers are listening from home, only know the song that JUST ended. They cannot reference/predict/ask about the upcoming track. MM and Megan can tease what's coming from the studio side; the caller can't. |
| **4.4.5** | **Metadata-override cascade fix.** Get Info edits were wiping each other. Cause: save handler required fingerprint match to merge; after applying edit 1 the in-memory track's fingerprint changed, so edit 2 saw a mismatch and overwrote the whole entry. Now always merges into the existing entry's `fields` object, only the latest fingerprint is updated. |
| **4.4.6** | **Airfoil rattle fix v1.** Long Radio sessions accumulated 80-200+ dead `MediaElementSource` nodes connected to preamp (TTS clips, never explicitly disconnected). Web Audio's per-sample loop still processed them → CPU pressure → Airfoil's network resampler turned that into audible rattle. `attachClipToBroadcast` and `attachAnnouncerToBroadcast` now register `ended`/`error` listeners that disconnect the source immediately on clip end. |
| **4.4.7** | **Songs sidebar freeze fix.** Pure regression from 4.4.5 — copy-paste typo: I used `libState.tracks` in `SongsView` (where the destructured name is `lib`, not `libState`). useCallback dep array threw a ReferenceError every render → infinite loop. Renamed to `lib.tracks`. |
| **4.4.8** | **Airfoil rattle fix v2.** Same disconnect-on-end treatment for music Howls (`detachHowlFromEq` called from useAudio.ts wherever the active Howl is replaced). Plus `window.__resetAudio()` escape hatch — call from dev console to fully rebuild the audio chain if anything ever desyncs. |
| **4.4.9** | **Silent music regression from 4.4.8 fix.** Howler uses an HTMLAudio *pool* — the same element gets reused across tracks. `detachHowlFromEq` disconnected the source on unload but kept the `boundSources` WeakMap entry (you can't re-bind a `MediaElementSource` — throws). Next attach saw the entry and skipped reconnection → source disconnected, element routed to nowhere, music silent. Fixed: if entry exists, just **reconnect** the existing source to preamp instead of trying to re-bind. |
| **4.4.10** | **No more accidental library-wipe from playlist context menu.** Right-click in playlist used to have both "Remove from Playlist" and "Delete Song" — one click apart. Latter wiped from the entire library. Now playlist context menu only has "Remove from Playlist." Library deletion still works from main Songs view (unaffected). |
| **4.4.11** | **Get Info: drag-select no longer clobbered by auto-select-on-navigate.** The 50ms `firstInputRef.current?.select()` timer fired on every prev/next navigation AND on initial mount. If the user mouse-downed in the Name input within that 50ms window and started a drag-select, the timer fired mid-drag, programmatically selected all the text, and destroyed the user's in-progress selection. Now: initial mount still selects-all (iTunes Get Info convention — type to overwrite the title); prev/next navigation focuses the first input without selecting; either way, the auto-select cancels itself if the user mousedowns or focuses any input first (capture phase, fires before the input handler). User reports drag broken on ALL fields, not just Name — if that persists after this fix, a separate cause exists (likely CSS or a Chromium quirk we haven't found). See `docs/phase-a-verification.md` for the full investigation. |
| **4.4.12** | **Embedded album art on import + artwork-index disk-write hardening + sips-failure surfacing.** Five linked changes. (1) `import-track` and `import-tracks` IPC handlers now read `metadata.common.picture[]` from `music-metadata` parse and save the front-cover into the artwork dir alongside the track, mirroring the `set-custom-artwork` conventions (JPG passthrough, `sips` convert for PNG/TIFF/etc., versioned hash for cache-bust). Factored into `extractAndSaveEmbeddedArtwork` helper. Identity-gated: never overwrites an existing index entry. Result passes `{key, hash}` back to renderer; `App.tsx` dispatches `ADD_ARTWORK` in the same React batch as `ADD_IMPORTED_TRACKS` so the cover shows up on the first render alongside the row. (2) `saveArtworkIndex` now writes to a unique tmp filename and atomic-renames, so a mid-write crash can't leave a torn JSON. (3) `saveArtworkIndex` calls are serialized via a single-flight Promise chain (`artworkWriteChain`) — same pattern as the metadata-overrides fix in 4.1.1. Concurrent IPC calls during a heavy import batch can no longer interleave and lose entries. (4) When `set-custom-artwork` returns `ok:false` (typically `sips` failing on a corrupt/unsupported image), all 6 view-level `handleSetCustomArt` handlers + `AlbumArtPanel` + the inline "Add Artwork…" context-menu actions now call `setNotice(...)` from the activity store. `NowPlaying.tsx` renders the notice as a 4th LCD-pill mode (in addition to playing / rip / sync) with auto-clear after 4 sec. Previously the renderer silently skipped `ADD_ARTWORK` but the user already saw the Get Info preview from `localArtHash` and assumed it stuck. (5) One-shot embedded-art backfill on app launch: new `backfill-embedded-artwork` IPC walks every track in the library, parses each file, extracts embedded art for albums missing artwork, yields between tracks (`setImmediate`, 4.0.10 pattern), and writes a marker file at `userData/artwork-backfill-done` so it only runs once. Progress events stream to the renderer every 25 tracks. See `docs/phase-a-verification.md`. |
| **4.4.13** | **Per-view scroll position persistence within the session.** Bug report: scroll halfway down Songs → switch to Artists → switch back to Songs → list jumps back to the top. Root cause: `MainContent.tsx` switches views via a switch/case that returns one component at a time, so `SongsView` (and every other view) fully unmounts when you navigate away. `useVirtualScroll`'s internal `scrollTop` `useState` resets to 0 on the remount, and the freshly created container div has `scrollTop: 0`. Nothing was preserving the position. Fix: new `src/renderer/hooks/useScrollPersistence.ts` — a `useLayoutEffect`-based hook backed by a module-level `Map<string, number>` keyed by view (e.g. `'songs'`, `'albums'`, `'playlist:<id>'`, `'smart-playlist:<id>'`). On mount, the hook reads the saved `scrollTop` and writes it onto the container ref **before** the browser paints (so the user never sees the scrollTop=0 flash); a passive `addEventListener('scroll')` keeps the cache fresh on every scroll, including programmatic writes from `scrollToIdx`/auto-follow. The key is reactive — switching playlist A→B inside `PlaylistView` triggers a fresh restore from B's cached value, so each playlist remembers its own position independently. Wired into all 6 library views (`SongsView`, `PlaylistView`, `SmartPlaylistView`, `ArtistsView`, `AlbumsView`, `GenresView`). Cross-launch persistence (writing to disk and restoring on app start) is **out of scope** — that's Phase C; this fix is in-session only, which is what 90% of the bug feels like. Known limitation: `GenresView.tsx`'s root uses flex layout — the inner column lists and tracklist scroll internally, not the root — so the hook is a no-op on Genres in the current layout. The hook is wired anyway for symmetry; a follow-up can persist the inner scroll containers if the user reports it. Out of scope: the `auto-follow-on-sorted-change` suspect in `SongsView.tsx:408-416` — the existing 5-second idle gate appears to handle it; revisit if "scroll resets unexpectedly when idle" is reported. |
| **4.4.14** | **DJ Mode cancel side-effect audit + two leak fixes.** Per the CLAUDE.md footgun "Every cancel/undo/stop path must reverse all side effects of the corresponding start path", audited the three DJ Mode cancel paths in `Toolbar.tsx` against the start path. Full ledger at `docs/dj-mode-cancel-audit.md`. Two leaks fixed: **(LEAK 1, Airfoil-rattle class)** `attachClipToBroadcast` / `attachAnnouncerToBroadcast` register `ended`/`error` listeners that disconnect the `MediaElementSource` from `preampNode` on natural clip end (4.4.6 fix). Cancel paths called `djAudioRef.current.pause()` and nulled the ref, but `pause()` does NOT fire `ended` — so the source node stayed connected and accumulated into the broadcast graph forever. Over a session of rapid DJ-Mode toggling or manual-track-skips mid-banter, dozens of dead source nodes piled up, exactly the same CPU-pressure pattern 4.4.6 fixed for the natural-end case. New helper `detachClipFromBroadcast(audio)` in `src/renderer/audio/eq.ts` disconnects from the broadcast chain (works for both preamp-direct and the announcer-FX chain since both share the `boundSources` WeakMap) and deletes the entry. Called from all three DJ Mode cancel paths before nulling `djAudioRef.current`. **(LEAK 2, rapid-toggle race)** If the user toggled DJ Mode off→on within ~500ms while `musicmanDjSet` or `musicmanSpeak` was in flight, the cancel's `djCancelledRef.current = true` was then reset to `false` by the re-click BEFORE the old IPC resolved. The stale response then proceeded as if it weren't cancelled, dispatching state changes that interleaved with the new run. Fix: added `djModeGenerationRef` integer counter; each `startDjSet` captures `myGen = ++djModeGenerationRef.current` at top and checks `if (djModeGenerationRef.current !== myGen) return` after every await. Cancel handlers bump the counter, invalidating any in-flight run. `djCancelledRef` is kept as belt-and-suspenders for the simple toggle-off case. Items audited and found clean: volume restore, autoDjMode flag clearing (eager + ack handshake is synchronous), Howl ducking (DJ Mode is dialog-in-silence per 4.3.2 so nothing to un-duck), `dj-mode-state` CustomEvent broadcast (fires automatically on `setDjModeActive(false)`), Stephen Hands sidebar Picks panel (not coupled to DJ Mode state). Out-of-scope follow-ups noted in the audit doc: global `isFadedRef` cross-component lifecycle, `djExiting` timer not cleared on cancel, DJ Mode not firing `musicman-speaking-start`/`-end` events. |
| **4.4.15** | **Output-device disconnect UX: banner + auto-reconnect + fallback notice.** User reports "AirPlay always breaks down" and "Airfoil mid-song stutter." 4.0.6 / 4.1.5 / 4.4.6 / 4.4.8 / 4.4.9 patched the audio-graph side (source-node disconnects, Howler pool reuse, broadcast-FX bind fallback). What was missing was user-visible feedback when the active AirPlay/Bluetooth/external device drops mid-playback — JakeTunes kept playing into the void with no indication anything was wrong. This adds three layers, all reusing the activity-store `setNotice` + LCD-pill mode 4 from 4.4.12: **(a) Poll-based disconnect detection.** While `pb.isPlaying && isExternalOutput && !airplayOpen`, `Toolbar.tsx` polls `list-audio-devices` IPC every 5 sec. When the previously-active external device disappears from the list (Core Audio has rerouted to internal), surfaces `"Output → internal speakers (<device name> unavailable)"` for 6 sec. Polling is gated on the three conditions so paused/internal/menu-open windows have zero IPC overhead. **(b) Auto-reconnect on next track start.** A `useEffect` keyed on `pb.nowPlaying?.id` checks if `lastExternalDeviceRef.current` is back in the device list, and if so calls `set-audio-device` to switch back. Notice: `"Reconnected to <device name>"`. **(c) Per-device cooldown.** `reconnectAttemptedRef = Map<deviceId, lastAttemptMs>`. Reconnect is skipped if attempted within the last 30 sec for that device id — prevents reconnect-loops on a flaky receiver. Per-device (not global) so two different AirPlay receivers each have their own cooldown. Device identity is the stable numeric `id` from the audio helper, not name (per twin-audit: "Living Room" can be two different receivers). `lastExternalDeviceRef` is auto-cleared when the user manually selects a builtin device (intent: stay on internal). **Out of scope:** Bonjour/mDNS auto-discovery of NEW AirPlay devices (per CLAUDE.md "Out of Scope (current phase)"); cross-launch persistence of preferred device (Phase C); settings toggle to disable auto-reconnect (defer, build feature first). |
| **4.4.16** | **iPod eject button no longer silently fails.** User reported "Eject button does nothing." Two bugs. **(1)** `Sidebar.tsx`'s eject-button onClick fired `window.dispatchEvent(new Event('jaketunes-ipod-ejected'))` unconditionally in the `.then()` — regardless of whether the IPC returned `{ ok: true }` or `{ ok: false, error: ... }`. When eject failed (most common cause: a track from the iPod was currently playing, so `diskutil eject` returned "Resource busy"), the sidebar still fired the ejected event, the UI briefly flickered, then re-detected the still-mounted device and rendered it again. From the user's POV: nothing happened. Fixed: button awaits the result, only fires the ejected event on `ok:true`, calls `setNotice` (4.4.12 LCD-pill notice) with the actual error message on `ok:false`. **(2)** The `eject-ipod` IPC handler bailed with "No iPod detected" if `detectedIpodMount` was null — but it never probed `findIpodMount()` like its peer handlers (`readIpodDatabase`, `check-ipod-mounted`) do. So if the module-level state desynced from disk reality (after sleep/wake, after a sync remount, after the renderer hadn't called `check-ipod-mounted` recently), eject silently refused even though the device was right there. Fixed: handler now probes `findIpodMount()` first if state is null, matching the existing pattern. **Out of scope (follow-up):** auto-pause iPod-sourced playback before attempting eject. Currently if the user is playing a track from the iPod, eject correctly fails with "Resource busy" via the new notice, but UX would be smoother if JakeTunes paused first. Deferred — the notice is enough to ship tonight. |
| **4.4.17** | **Eject auto-stops iPod playback first.** The 4.4.16 follow-up. If `pb.isPlaying && pb.nowPlaying.path.startsWith('iPod_Control:')`, the sidebar eject handler now dispatches `{ type: 'STOP' }` to PlaybackContext before invoking the eject IPC, then awaits 500ms for Howler's unload to release the `ipod-audio://` streaming file handle. After that, `diskutil eject` succeeds cleanly instead of returning "Resource busy". If something goes wrong and eject still fails (some other handle held by JakeTunes or another process), the 4.4.16 notice path still surfaces the actual error. STOP (not PAUSE) is used because PAUSE only flips `isPlaying:false` while keeping the track loaded — the file handle would stay open. STOP additionally nulls `nowPlaying`, which causes `useAudio.ts` to unload the Howl. 500ms is comfortably more than the local unload takes and shorter than user-perceptible delay. Eject button now Just Works regardless of playback state. |
| **4.4.18** | **Seamless multi-device sync + Qobuz/Bandcamp auto-import pipeline.** Two big wins shipped together because they're one architectural story: JakeTunes is now the orchestrator of its own propagation across devices. **(1) JakeTunes-internal sync trigger.** Replaces the original `com.jaketunes.sync` LaunchAgent, which hit a fundamental macOS Sequoia limitation: launchd-domain processes are kernel-level blocked from network volumes (every workaround tried — `SessionCreate=true`, `osascript do shell script`, `launchctl asuser $UID` — failed at the `open()` syscall). New `src/main/sync-orchestrator.ts` runs `~/bin/jaketunes-homemini-sync.sh` from JakeTunes' main process, which inherits the user's GUI session permissions and can access `/Volumes/JakeShared` cleanly. Triggers wired into `import-track`, `import-tracks`, `save-metadata-override`, and `save-playlists` IPC handlers post-success; safety-net `setInterval(10 min)` for out-of-band edits. Single 30-second debounce coalesces album-sized bursts (a 12-track Qobuz drop fires ONE sync, not 12). Single-flight gate prevents two concurrent syncs; any trigger that lands during an in-flight sync queues a fresh run for the moment the current one ends, so the final state is always synced. Outcomes are forwarded to the renderer via `library-sync-status` IPC and surfaced via the activity-store `setNotice` (LCD-pill mode 4 from 4.4.12) — failures always shown; successes shown only for explicit triggers (import / edit / playlist), not the 10-min safety-net tick (would be visual noise). The launchd plist is tombstoned with a full explanation comment so a future maintainer doesn't try to re-enable it. **(2) Expanded sync surface.** The sync script now propagates not just `library.json` but also `metadata-overrides.json` and `playlists.json` — the three files that comprise per-device library state. `chat-history.json` is deliberately excluded (per-device Music Man conversation history is user-state, not library-state). Single-pass `rsync -tz` over Tailscale ssh, gated on a mtime-fingerprint diff so we only push when something actually changed. **(3) Inbox auto-import pipeline (Qobuz / Bandcamp / drag-drop into folder).** New `src/main/inbox-watcher.ts` — chokidar watches `~/Music2/_inbox` recursively with `awaitWriteFinish: 3000` (waits 3 sec after last byte before considering a file ready — handles download-in-progress). 1.5-sec batch debounce coalesces album-sized adds. Audio files only; `.crdownload` / `.part` / `.tmp` / dotfiles ignored. Detected paths route to the renderer via `inbox-files-detected` event; renderer pipes them into the SAME `enqueueFiles()` drag-drop uses, with `deleteSourceOnSuccess: true` so the source FLAC is removed from the inbox after JakeTunes has its transcoded copy in `iPod_Control`. Path-gated `delete-inbox-source` IPC (refuses to delete anything outside the currently-watched inbox — safety against renderer state corruption asking for arbitrary rm). Idempotent watcher (reconfigure on settings save is a no-op when nothing changed). Settings UI: Preferences → Library → Inbox (toggle + path picker, defaults to `~/Music2/_inbox`, default-on). **End-to-end:** Qobuz finishes downloading FLAC → file lands in inbox → 3 sec later chokidar fires → renderer auto-imports (existing pipeline, embedded artwork extracted via 4.4.12, library.json updates) → `triggerSync('import')` fires → 30-sec debounce → sync script runs → music + JSON state ship to homemini over Tailscale → homemini JakeTunes restarts → tracks visible everywhere. Total wall-clock for a small album drop: ~45 sec. **Out of scope (Phase 2):** bidirectional sync (edits on homemini → laptop), server-side library as source of truth, real-time push (SSE/WebSocket), iOS app. This ship handles 90% of the seamless feeling without the multi-week infrastructure investment. |
| **4.4.19** | **Phase E — Home / Dashboard view (first ship: Recently Added + Top Artists).** Per the phase plan (handoff doc and Jake's explicit ask), JakeTunes now has a Home view as the top entry under LIBRARY in the sidebar. First ship covers two of the planned sections — future versions add Listening Stats, Picks aggregator, Music News, Bandsintown integration. **Recently Added** is a horizontal-scrolling row of up to 12 album cards, sorted by the newest `dateAdded` among the album's tracks (so a single re-imported bonus track bumps the whole album to the top — feels right). **Top Artists** is a horizontal row of up to 10 artist circles, sorted by aggregate `playCount` across all their tracks; artwork is the first available cover among their albums (falls back to a two-letter initial badge). Clicking an album plays it; right-click drills into Albums view. Clicking an artist drills into Artists view. "See All" buttons jump to the Recently Added smart playlist and Artists view respectively. New files: `src/renderer/views/HomeView.tsx` (single self-contained component with two `useMemo` aggregators against `lib.tracks` — no separate state, no sync), `src/renderer/styles/home.css` (card-based, iTunes-era visual language: soft shadows, plenty of whitespace, scroll-snap horizontal rows, circular artist art per iTunes 11+ convention). Wired through `ViewName` type extension, `LIBRARY_ICONS` map, `libraryItems` sidebar list, and `MainContent` switch. New `HomeIcon` (gable-roof house silhouette in warm orange — distinguishable from the cooler Songs/Artists/Albums/Genres icon set). **Known limitations:** clicking an album currently plays it but doesn't drill into AlbumsView with that specific album expanded — drill-down to a focused album is a future iteration. Clicking an artist drills into the generic Artists view, not pre-scrolled to that artist. Both are acceptable for v1 because the cards themselves play directly and the "See All" affordance gets users to the full views. |
| **4.4.20** | **Home view: long album titles no longer stretch the card width.** Bug from 4.4.19: the "Herb Alpert Presents Sergio Mendes & Brasil '66" card rendered roughly twice as wide as adjacent cards because `flex: 0 0 156px` alone wasn't enough to pin the card width when a `white-space: nowrap` title sat inside it. Flex items default to `min-width: auto`, which means a long nowrap text element pushes its parent (and grandparent, and the card itself) wider than its declared flex basis — `text-overflow: ellipsis` then never kicks in because the element already grew to fit the content. Fix: belt-and-suspenders width pinning in `home.css` — explicit `width: 156px; max-width: 156px; box-sizing: border-box` on `.home-album-card` plus `width: 100%; max-width: 100%; min-width: 0; overflow: hidden` on every text descendant in the card column (`.home-album-info`, `.home-album-title`, `.home-album-artist`). Same treatment for the artist cards (`.home-artist-card`, `.home-artist-info`, `.home-artist-name`). The CSS comment block explains the gotcha so a future maintainer doesn't reintroduce it. **No JS/TS changes** — purely CSS layout. The data layer was already correct (one card per album was always the intent; verified there's exactly one library.json track for the album in question). |
| **4.4.21** | **Home view polish pass — six small fixes targeting the "feels glitchy" feedback.** Jake said the Home view "feels a little glitchy, not crazy glitchy just not A+." Identified six likely culprits, shipped all of them in one CSS-mostly pass: **(1)** Removed `scroll-snap-type: x proximity` from the card row — felt jerky on trackpad flicks since it snapped to card boundaries instead of allowing fine-grained free scroll. Apple Music / iTunes 11+ use free scroll for the same UI, this matches. **(2)** Image fade-in: `.home-album-art img` and `.home-artist-art img` start at `opacity: 0` with a 220ms ease-out, JSX `onLoad` flips them to `opacity: 1` via the `home-album-art-loaded` / `home-artist-art-loaded` classes. No more pop-in when the album-art:// scheme handler is slow on first paint. **(3)** Integer font sizes — `font-size` was 12.5 / 11.5 / 10.5 px (fractional → fuzzy hinting on non-retina pixel boundaries). Bumped to 13 / 12 / 11 px with explicit `line-height: 1.25-1.3` for consistent vertical rhythm. **(4)** Calmer hover — dropped the `translateY(-2px)` bounce on the card root (it caused micro-jitter during horizontal scroll because the card briefly moved up and back). Replaced with a subtler effect targeted just at the art element: 180ms `box-shadow` + `transform: scale(1.02)` lift on hover, `scale(0.985)` press on active. Same treatment for artist cards (`scale(1.04)` / `scale(0.97)`). **(5)** Scroll persistence — Home view now uses the `useScrollPersistence` hook (shipped in 4.4.13 but never wired into HomeView), keyed `'home'`. Switching to another view and back preserves vertical scroll position. **(6)** Click acknowledgment — clicking an album card to play now adds an `is-playing-flash` class for 380ms that draws a 2px blue ring around the art via `box-shadow: 0 0 0 2px rgba(58, 114, 196, 0.85)`. The click feels acknowledged before the audio engine actually spins up. Net effect: scroll feels smoother, art appears gracefully, type is crisper, hover doesn't fight the scroll, click feedback is immediate, and your scroll position survives view changes. Same data, same layout — purely interaction polish. |
| **4.4.22** | **Songs view scroll persistence actually works now (virtualization seed).** Bug Jake reported in 4.4.21: "if i go back to songs....it doesnt stay where i left off." 4.4.13's `useScrollPersistence` hook DID restore the DOM `scrollTop` before paint, BUT `useVirtualScroll` keeps its own React `useState` for the scroll position, defaulting to 0. So the DOM scrolled to the saved offset (e.g. 5000 px) but the virtual viewport rendered rows 0-29 (the only ones it knew about) — leaving blank space at the saved position until a real scroll event propagated back into React. By the time React caught up, the scroll position got clobbered. Fix: new `getSavedScrollTop(key)` export from `useScrollPersistence` that lets virtualized lists read the cache *synchronously, before mount*, and seed their internal state via a new optional `initialScrollTop` parameter on `useVirtualScroll`. `SongsView` passes `getSavedScrollTop('songs')` so the very first render of the virtual viewport computes `startIndex`/`endIndex` from the persisted position — rows at the saved offset are rendered immediately, no blank-space flash, no race with the scroll-event roundtrip. Other views were unaffected because they aren't virtualized — `PlaylistView`/`SmartPlaylistView`/`ArtistsView`/`AlbumsView` render their whole list into the scroll container, so the existing `useScrollPersistence` works on its own. `GenresView` still has the known limitation noted in 4.4.13 (its root uses flex layout, so root `scrollTop` is always 0). |
| **4.4.23** | **Home view horizontal scroll persistence — card rows remember where you scrolled to.** Jake's follow-up after 4.4.22: "home is not remembering where i was when i go back and forth. but songs does and artists does." Root cause: 4.4.21 wired `useScrollPersistence('home', rootRef)` on `.home-view` (the vertical scroll container), but the meaningful scroll on this view is the HORIZONTAL scroll inside each `.home-card-row` — scrolling right through Recently Added to see album #11, then bouncing to Songs and back, would reset the row to leftmost because the hook only tracked the wrong axis. Fix: widened `useScrollPersistence` to capture and restore BOTH `scrollTop` AND `scrollLeft` on every tracked element. The cache value is now `{top, left}` instead of a bare number; `getSavedScrollTop` keeps the same signature so `useVirtualScroll`'s seed (4.4.22) is unchanged. New `getSavedScrollLeft` for symmetry. Then `HomeView` got two new refs — `recentRowRef` and `artistsRowRef` — both wired through `useScrollPersistence` with distinct keys (`'home-row-recent'`, `'home-row-artists'`). The existing root-level `useScrollPersistence('home', rootRef)` still persists the (mostly vestigial) vertical scroll on the view shell, but the per-row hooks are what users actually feel. Setting `scrollLeft` on a non-horizontal scroller is a harmless no-op (clamps to 0), so the same widened hook works correctly on every existing site (`SongsView`, `PlaylistView`, `SmartPlaylistView`, `ArtistsView`, `AlbumsView`, `GenresView`) without changes. |
| **4.4.24** | **Motion polish pass — Home card stagger-in, view fade transitions, smoother row hovers.** Jake said "the scrolling needs a little more animation, a little more smoothness, all around." Three CSS-only fixes that target what reads as "snappy but not jarring": **(1) Home card stagger-in.** New `home-card-in` keyframe (`opacity: 0 → 1` + `translateY(6px → 0)`, 280ms `cubic-bezier(0.2, 0.65, 0.3, 1)`) applies to every `.home-album-card` and `.home-artist-card` on mount, with per-position delays via `:nth-child(N)` rules: 0ms, 28ms, 56ms, … up to 308ms for the 12th card. Each card lands ~28ms after the previous one — the whole row is in place in under 340ms, fast enough to feel snappy, slow enough to read as deliberate motion rather than a hard cut. **(2) View transition fade.** New `src/renderer/styles/main-content.css` defines a `view-mount-fade` keyframe (opacity 0→1, 140ms ease-out). `MainContent.tsx` now wraps its `switch (currentView)` in a single keyed `<div class="main-content-view">`, where `key = currentView` (plus the active-playlist-id when in playlist views, so switching between playlists also re-fades). The wrapper uses `display: contents` so it's invisible in the box model — children's `height: 100%`, flex behavior, etc. flow through to the parent unchanged. Every view-switch (Songs → Home → Albums, playlist A → playlist B, etc.) gets a 140ms fade-in. **(3) Smoother row hovers.** Added explicit `transition: background-color 90ms ease-out` to `.songs-row` and `.artist-row`, and `transition: transform 180ms cubic-bezier(0.2, 0.65, 0.3, 1), box-shadow 180ms …, background-color 120ms ease-out` to `.album-card` (was previously plain `0.15s` linear ease). Selection state changes now ease in/out instead of snapping. **All three respect `prefers-reduced-motion: reduce`** — the keyframes are cancelled and the system falls back to instant transitions for users who turn down motion in System Settings. No layout changes, no JS behavior changes. |
| **4.4.25** | **iOS-style elastic rubber-band overscroll on every scrollable list.** Jake's ask: "need the newer apple scroll animations when you hit the end of a menu." Chromium on macOS provides a native bounce, but it's tied to OS scroll-momentum and tends to feel weak in Electron — particularly for horizontal rows and when scrollbars are styled. New hook `src/renderer/hooks/useElasticOverscroll.ts` gives explicit control. It listens for `wheel` events with `{ passive: false }`, and when the user wheels past the start or end of a scroll container along the configured axis, it: captures the wheel delta with `preventDefault`, applies it as a damped `transform: translate(...)` on the container's content (the container itself for horizontal scrollers, `firstElementChild` for vertical — so the frame and scrollbar stay anchored, only the content shifts), uses **logarithmic resistance** (`1 - r * 0.7`, where r is normalized overscroll magnitude) so the further you push, the harder it gets, mirroring iOS's "rubber band stiffens" feel, caps overscroll at 40% of the container's relevant dimension, and on wheel-quiet (~90ms with no new wheel events) springs back to 0 over 360ms via `cubic-bezier(0.16, 1, 0.3, 1)` — the same easing curve Apple uses in the Music app's spring-back. If the user resumes scrolling within bounds, the spring-back fires immediately so cancellation feels responsive. The hook also sets `overscroll-behavior: contain` on the container while mounted so we don't double up with Chromium's native effect; previous value restored on unmount. Cleanup also clears transform/transition styles on unmount so view-switches don't leave stale overscroll state. Wired into: **HomeView** (both Recently Added and Top Artists horizontal rows + root vertical), **SongsView** (vertical, via the virtualization container), **AlbumsView** (vertical, root), **ArtistsView** (vertical, root), **PlaylistView** (vertical, songs body), **SmartPlaylistView** (vertical, songs body). Six scroll containers, one hook call each — tight surgery. |
| **4.4.26** | **Rubber-band tuning — "sooooo stretchy" → subtle.** Jake on 4.4.25: "rubberband works but its soooo stretchy." Three defaults in `useElasticOverscroll` tightened: `damping` 0.35 → **0.18** (each wheel tick contributes about half as much visual translation — the boundary registers as a brief acknowledgment, not a stretch you'd photograph), default `cap` 40% → **12%** of container dimension (max stretch is roughly "an album cover's worth" on a typical container, not "halfway off-screen"), resistance multiplier `1 - r * 0.7` → **`1 - r`** (fully tapers to zero at the cap, so a fast trackpad flick can't punch visibly past the cap regardless of velocity). Spring-back duration trimmed 360 → 320ms for a slightly snappier return. No structural changes — same hook, same wiring, same six call sites. Hook's options interface still exposes `damping` / `cap` / `springMs` if any individual view wants to override. Comments in the source spell out exactly what the previous numbers were and why the new ones are better, so a future tuning pass has the context. |
| **4.4.27** | **Removed the JS rubber-band, restored native macOS bounce; clicking an artist on Home now drills into THAT artist.** Two unrelated fixes shipped together. **(1) Rubber-band rip-out.** Jake on 4.4.26: "rubberband is bad. slow. not good. very poor." The custom `useElasticOverscroll` hook (4.4.25 + 4.4.26 tuning) was both intercepting wheel events with `passive: false` + `preventDefault` AND disabling Chromium's native macOS bounce via `overscroll-behavior: contain` — replacing OS-quality bounce with our worse approximation. Removed all six `useElasticOverscroll(…)` call sites (HomeView × 3, SongsView, AlbumsView, ArtistsView, PlaylistView, SmartPlaylistView), kept the hook file in case we want to revisit in the future, restored Chromium's native macOS scroll bounce for free. **(2) Artist drill-in from Home.** Promised in the 4.4.19 "Known limitations" section and finally wired. New utility `src/renderer/utils/drillIn.ts` — module-level `pending` slot for cross-view "drill into specific item" handoffs. Pattern: source view calls `requestDrillIn('artist', name)` then dispatches `SET_VIEW`; destination view calls `consumeDrillIn('artist')` on mount, gets the key once (cleared), expands + `scrollIntoView({block:'start', behavior:'smooth'})`. Stale entries (>5 sec) are discarded to prevent a forgotten request from firing weeks later. `HomeView` artist-card `onClick` now uses this; `ArtistsView` has a mount `useEffect` that consumes and matches with exact-then-case-insensitive fallback (covers the rare case where HomeView's `albumArtist || artist` keying differs from ArtistsView's `artist`-only grouping). If no match, graceful fallback to the generic Artists view. Same module is set up to handle `'album'` drill-ins later (AlbumsView, future). |
| **4.4.28** | **Home view: Music News + New This Week sections.** Two new sections on the Home dashboard, fed by the existing RSS infrastructure in `src/main/external.ts` (4.3 era) — Pitchfork Best New Albums, Stereogum news feed, The Quietus. Previously this data was only used by Music Man's prompt-building (`getRecentReviews()` returned `[Source] Title` strings). New parallel function `getMusicNews()` and `getNotableReleases()` parse the same feeds into structured items: `{ title, link, source, pubDate, imageUrl?, isReleaseReview }`. Both share a single 1-hour parsed cache across all three feeds (so HomeView calling both handlers triggers ONE network round-trip per hour). Image extraction tries `<media:content>`, `<media:thumbnail>`, image `<enclosure>`, and falls back to the first `<img>` inside `<content:encoded>` / `<description>`. **Notable Releases** ("New This Week") = Pitchfork BNA only, rendered as **cover-led** cards (156×156 album art, title underneath, "today" / "yesterday" / "N days ago" date). **Music News** = Stereogum + Quietus, rendered as **text-led** cards (220px wide, optional 100px-tall hero image, uppercase source/date meta, headline clamped to 4 lines via `-webkit-line-clamp`). Both card types use the same `home-card-in` stagger animation as the existing rows. Click any card → `open-external-url` IPC → `shell.openExternal` opens the article in the user's default browser. The IPC is allowlisted to `http(s)://` schemes so a corrupted renderer can't ask main to open arbitrary `file://` or custom-scheme URLs. Both rows persist their horizontal scroll position via the existing 4.4.23 hook (`home-row-news` and `home-row-releases` keys). Sections only render when there's at least one item to show — empty/loading state hides cleanly. |
| **4.4.29** | **Home polish — readable headlines, quality news sources, welcoming header.** Three issues Jake flagged on 4.4.28. **(1) Headlines showed raw HTML entities.** Titles like `&#8220;Weird Al&#8221; Broadway Musical In The Works` rendered verbatim because RSS feeds embed curly quotes/apostrophes/ampersands as numeric character references and my parser passed them through unchanged. New `decodeEntities()` helper in `external.ts` covers named (`&amp;`, `&lt;`, `&gt;`, `&quot;`, `&apos;`, `&nbsp;`), decimal numeric (`&#8220;`), and hex numeric (`&#x201C;`) entities; applied to titles in the structured-feed parser. **(2) Stereogum news = clickbait.** "Netflix Roasted Kevin Hart Because Paul McCartney Turned Them Down" is exactly the kind of headline Jake doesn't want on his music dashboard. Swapped Stereogum's main news feed for higher-signal sources: **Pitchfork Features** (long-form criticism), **NPR Music** (All Songs Considered), **The Quietus** (UK underground/electronic — kept), **Aquarium Drunkard** (deep indie / Americana). Pitchfork "Best New Albums" still drives the cover-led "New This Week" row. Five feeds total now, all parsed in parallel under the same 1-hour shared cache. **(3) Home header felt flat.** Replaced "JakeTunes · 5,498 tracks" with a welcoming header: time-of-day greeting ("Good morning, Jake." / "Good afternoon" / "Good evening" / "Up late" — cycles by hour), today's date in long form ("Tuesday, May 12"), Brooklyn weather (when `OPENWEATHER_API_KEY` is configured — wiring's been in `external.ts` since 4.3 for Music Man's prompts, just exposed via new `get-brooklyn-weather` IPC), and the library-stats line dropped to a subtitle. Header gets a subtle warm gradient backdrop matching the orange Home sidebar icon. Weather glyphs are SVG-friendly Unicode (`☀ ☁ ☂ ❄ ⚡ ≋`) chosen by OpenWeather condition string — feels like a Mac app, not an emoji-filled webpage. Falls back to date-only header gracefully if the weather IPC returns null (no API key, network failure). |
| **4.4.30** | **News row actually shows news now — and the "via" header reflects reality.** Jake on 4.4.29 with a screenshot: "this doesn't look like news." Correct — Aquarium Drunkard is a music *blog* about obscure reissues and Peel sessions; NPR Music posts Tiny Desk announcements and TV reviews; The Quietus is long-form criticism; Pitchfork Features is essays. All four are high-quality, none of them are "news." Swapped the news-row source list to dedicated news RSS feeds: **Pitchfork News** (`pitchfork.com/rss/news/` — separate from Features), **Stereogum New Music** (`stereogum.com/category/new-music/feed/` — release/single announcements, not the clickbait main feed), **Brooklyn Vegan** (tour + release news), and **Consequence** (broad music news). Pitchfork "Best New Albums" still drives the cover-led "New This Week" releases row — that section was fine, only the news row needed the fix. **Also fixed the stale "via Stereogum, The Quietus" header** that was still showing under "Music News" even after the 4.4.29 source swap. It's now derived from the actual items: `via Array.from(new Set(news.map(n => n.source))).slice(0, 4).join(', ')` — so if the upstream feed mix ever changes again, the attribution updates itself instead of going stale. |
| **4.4.31** | **Music News goes 70% library-personalized + 30% universal; Consequence music-only.** Jake on 4.4.30 with another screenshot: "not all of this is good music news. music news should work 70% off of my library and 30% off of universally huge music news." Two fixes. **(1) Consequence main feed → music-only category.** Their `/feed/` includes TV/celebrity coverage (Pete Davidson roasts, Kimmel political jokes). Swapped to `consequence.net/category/music/feed/` so we only get music posts from them. **(2) 70/30 personalization in `HomeView`.** New `personalizedNews` memo: builds a Set of the user's top 200 artists by aggregate playCount (with a `+ 1` baseline so unplayed library artists still count — just ranked below played ones); pre-compiles a word-boundary regex per artist (escaped for special chars; artists with names shorter than 3 chars are skipped to avoid "Of" or "Si" matching random words); tags each news item as `relevant` if its title matches any pattern, `universal` otherwise; takes top 7 relevant + top 3 universal (sorted by pubDate desc within each bucket) for the rendered row. If `relevant` is short, tops up from `universal` so the row never looks empty. Net effect: most of the cards mention artists you actually listen to, with a few "what's happening generally in music" to round out the row. Renderer-side computation only — no IPC changes, no new main process state. The personalization recomputes on `[news, lib.tracks]` so changes to your library (new imports, removed tracks) immediately re-shape the row. |
| **4.4.32** | **Gossip filter on news + Bandsintown tour dates (100% library-personalized).** Jake on 4.4.31: "the news is still so gossipy. dont like it. we also need bandsintown api or similar 10000% based on my library." Two things. **(1) Gossip filter.** Even Pitchfork News and Stereogum's music-only category publish "X Reacts To Y" / "X Responds To Z" / "Watch X React" content that reads as celebrity drama. New `GOSSIP_PATTERNS` regex array in `external.ts` matches specific drama-coverage phrasing: `\breact(?:s|ed|ing)?\s+to\b`, `\bresponds?\s+to\b`, `\bclap[\s-]?back\b`, `\bcalls?\s+out\b`, `\bslam(?:s|med|ming)?\b`, `\broast(?:s|ed|ing)?\b`, `\bbeef\s+with\b`, `\bfires?\s+back\b`, `\bjokes?\s+(?:about|that)\b.*\b(?:Disney|Trump|GOP|politics|political)\b`, `\bdating\s+rumors?\b`, `\bweighs?\s+in\s+on\b`, etc. — narrow enough that real music news survives, broad enough that "Kimmel Jokes That His Political Turmoil Cost Disney Billions" gets dropped. Applied inside `fetchStructuredFeed` BEFORE caching, so the parsed-feed cache itself stays clean. Pitchfork "Best New Albums" is exempt — those are real album release announcements regardless of phrasing. **(2) Bandsintown tour dates.** New `getBandsintownEventsForArtist(artist)` + `getTourDatesForArtists(artists)` in `external.ts`. Uses Bandsintown's free Discovery API (`rest.bandsintown.com/artists/{name}/events?app_id=…`) — no API key required, just an `app_id` string for attribution (`jaketunes-desktop`). 8-concurrent throttle so we don't trip rate limits even on a fresh library with 60+ unique artists. Per-artist 24h cache; aggregate cache keyed by the sorted artist-set hash (so a stable top-N list returns instantly until library order changes). New IPC `get-tour-dates` picks the user's top 60 artists by aggregate playCount (`+ 1` baseline so unplayed library entries still count), runs the throttled fan-out, returns events sorted by date asc. Cold-cache call takes ~3-8 sec; warm cache is instant. New `Coming to a Stage Near You` section on Home renders the dates as **ticket-stub cards**: warm tan-and-orange date stamp on the left (`MAY / 22`), artist + venue + city on the right. Distinct from the album/release/news cards so the row reads at a glance as "stuff happening live." Click a card → opens the Bandsintown event page in your default browser. The card row fetches independently of news/releases so the cold-cache Bandsintown call doesn't block the rest of Home from rendering. |
| **4.4.33** | **Home flair — Featured Album hero + Listening Stats strip.** Jake: "the home page needs a little more flare. its boring." Two new sections at the top of Home that turn it from a list of horizontal rows into a real dashboard. **(1) Featured Album hero** ("Today's pick from your library"). Big 220×220 album art on the left, title + artist + meta on the right, big orange "Play Album" primary button + a secondary "More from {artist}" that drills into ArtistsView via the existing 4.4.27 drill-in utility. Pick logic: top 50 albums by aggregate `playCount` (filters out the long-tail of single-track no-name imports), then day-of-year seed (`floor(Date.now() / 86_400_000) % pool.length`) so the SAME album shows all day, a DIFFERENT one tomorrow — feels like a curated pick, not random refresh-roulette. Falls back to most-recently-added if the library has no play history. Hover on the art darkens it and overlays a play triangle; click anywhere on the art or the primary button plays. Subtle gradient backdrop (warm orange → cool blue) blends with the rest of the page. **(2) Listening Stats strip** — 4 small cards in a CSS grid: Total Plays · In Your Library (total hours, with `k hrs` formatting for huge libraries) · Top Artist · Top Genre. All computed live from `lib.tracks` in a `useMemo`, no extra IPC, recomputes on every import. Cards hover-lift subtly like the existing horizontal-row cards. Section gracefully drops Top Artist or Top Genre if those fields are empty in the library so the strip never shows "None yet" placeholders. Both sections use the existing `home-card-in` stagger animation so they feel like part of the family. No section title — the LABEL on the hero ("Today's pick from your library") and the stats labels themselves carry the affordance. |
| **4.4.34** | **"On the Horizon" — upcoming releases that haven't come out yet, 100% library-personalized.** Jake explicitly asked for this two ships back: "add upcoming new releases that havent come out yet!!" Built on top of MusicBrainz's release-group catalog, which carries `first-release-date` fields including future dates that major-label artists pre-register. New `getUpcomingReleasesForArtists(artists)` in `external.ts`: takes the user's top 60 library artists, batches them into Lucene-OR groups of 25 per query (so 60 artists = 3 queries, not 60), parallelizes the batches, dedupes by MBID across batches, returns release-groups whose `first-release-date` is in the future. Skips non-album primary types (audiobook, spokenword, "other") but keeps singles/EPs since those count as newsworthy from a favorite artist. Partial dates (`2026`, `2026-09`, `2026-09-15`) are tolerated — formatted as "2026", "September 2026", or "Sep 15" in the renderer. 24-hour aggregate cache keyed by sorted artist-set hash. Cover Art Archive serves the artwork at `coverartarchive.org/release-group/{mbid}/front-250` — many unreleased albums don't have art uploaded yet, so the renderer's `onError` swaps the broken image for a big-letter placeholder (first letter of the album title on a warm-tan gradient). New "On the Horizon" section on Home — cover-led cards like Notable Releases but with a date badge overlay in the bottom-left of the art (`Sep 15`, `Oct 2026`, etc.) so the "future-tense" framing is unmistakable. Click → opens the release-group on MusicBrainz in your default browser. Renders only when there's at least one match — many libraries (especially indie/underground-heavy ones) won't get any pre-registered upcoming releases from MB, which is fine; the section hides quietly. Fetches independently of news/tour-dates/releases so cold-cache MB calls don't block the rest of Home. |
| **4.4.35** | **Scroll perf — drop backdrop-filter, add GPU-layer hints, contain overscroll.** Jake: "scrolling doesnt feel smooth. vertically and horizontally." Three CSS-only targets, no behavior changes. **(1) Killed the 4.4.34 `backdrop-filter: blur(4px)` on the upcoming-release date badge.** Backdrop-filter re-blurs the pixels behind the element on every paint, and during scroll that's once per frame — multiplied by ~10 visible upcoming cards plus the vertical scroll happening above, that's a real budget hit. Replaced with `rgba(0, 0, 0, 0.7)` solid which reads the same visually and is essentially free to paint. **(2) `will-change: scroll-position` on every scroll container** — `.home-view`, `.home-card-row`, `.songs-body`, `.albums-view`, `.artists-view`, `.genres-column-list`, `.genres-tracklist`. Promotes each scroll surface to its own GPU compositor layer, so a scroll frame paints the scrolled content but doesn't trigger a parent reflow or repaint anything outside the scrollable area. **(3) `overscroll-behavior: contain` (or `-x: contain` for the horizontal rows)** on the same containers. Without this, hitting a scroll boundary chains the wheel/trackpad event to the parent scrollable — on Home the horizontal card row would try to scroll the vertical Home view when you ran past the right edge, which causes a brief jank as the browser reconciles ownership. `contain` blocks the chain WITHOUT blocking native macOS rubber-band (different mechanism), so the bounce still works. Net effect: trackpad flicks should glide on Home rows AND in the long Songs list. |
| **4.4.36** | **"Instant" sync — quick-mode rsync + 5-sec debounce.** Jake: "i uploaded on laptop and it needs to upload in the homemini instantly." The bottleneck was rsync's stat-walk over the full 73GB library on every import — even adding a single album spent ~5 min in `rsync -az --delete` discovery before pushing the ~50MB of new tracks. Two changes. **(1) `--quick` rsync mode.** The sync script now accepts a `--quick` flag. Quick mode: `find $LIBRARY_ROOT -mmin -10 -type f` enumerates files modified in the last 10 minutes, writes them to a temp file, then `rsync -rtz --files-from=$tmp` pushes only those. Cuts per-import sync from ~5 min to ~15 sec for a typical album drop. The full `rsync -az --delete` still runs from the 10-min safety-net tick so tombstones (deletions) and out-of-band edits propagate correctly. **(2) Sync orchestrator passes `--quick` for the high-frequency triggers** (`import`, `metadata-edit`, `playlist`); only the safety-net and manual triggers use full mode. **(3) Debounce dropped 30 sec → 5 sec.** The 30-sec window existed to coalesce 12 import-track triggers from a single album into one sync, but the single-flight gate already does that (additional triggers queue for after the in-flight sync finishes). 5 sec is enough to absorb the inbox-watcher's 1.5-sec batch debounce plus a margin, and makes "instant" feel achievable. End-to-end "drop file → homemini sees it" for a small album: ~10-15 sec, down from ~5 min. |
| **4.4.37** | **Artists view album rendering — actual art + full-width expanded layout + proper track rows.** Jake's screenshot showed J. Cole's *Born Sinner* expanded under his Artists view: empty placeholder square instead of the cover, and the tracklist stuffed into a 120px-wide grid column so song titles were ellipsis'd into oblivion ("Born Sinner (feat. J...", "She Knows (feat. A...", "Ain't That Some S..."). Two root causes, both fixed. **(1) Album art was never rendered.** The JSX literally had no `<img>` tag in `.artist-album-art` — always rendered the placeholder SVG. Now does a multi-candidate lookup against `lib.artworkMap` using the standard `${artistLower}|||${albumLower}` key — tries the artist as displayed, then each variant of `artist` / `albumArtist` from the tracks on the album, then falls back to the placeholder only if nothing matches. Same pattern AlbumsView uses; works for compilation albums where `albumArtist != artist`. **(2) Expanded-card layout breaks out of the grid.** Collapsed cards still sit in the 120px-wide `repeat(auto-fill, minmax(120px, 1fr))` grid (good — they're just thumbnails). When an album is toggled open, the new `.artist-album-card--expanded` class applies `grid-column: 1 / -1` to span the full row width, flex-rows the layout to art-on-left (fixed 140px) + info-on-right, drops a white-card background with a soft border. **(3) Track rows got a proper grid.** Was `display: flex` with just icon + title — couldn't align numbers or durations. Now `display: grid` with `grid-template-columns: 16px 28px 1fr 50px` so each row reads as `[now-playing icon] [track #, right-aligned] [title, ellipsis on overflow] [m:ss, tabular nums]`. Track numbers use `track.trackNumber` when set, fall back to the index. Track-row hover ease added (90ms — matches Songs view's 4.4.24 polish). All numeric columns use `font-variant-numeric: tabular-nums` so digits align cleanly across rows. |
| **4.4.51** | **Auto-route music off the computer speakers when a call starts — so you never pause for a Teams call again.** Jake: "find a way that i can play music on both my work computer speakers (line in pebble pro) AND any airplay speaker devices at the same exact time with absolutely no lag. so i dont have to pause music everytime i hop on a teams call." The literal ask — two outputs, same room, zero lag — isn't buildable: AirPlay has an inherent ~2-second receiver buffer, so a wired speaker and an AirPlay speaker playing the same stream are always ~2 sec out of phase. That's a property of AirPlay, not something software can tune away. So instead of fighting physics, JakeTunes sidesteps it: **one device at a time, switched automatically when a call grabs the mic.** **(1) Mic-activity detection (Swift).** `core/audio_helper` gets a new `mic-status` command — queries CoreAudio's `kAudioDevicePropertyDeviceIsRunningSomewhere` on the default *input* device, which is effectively "is the microphone live." Teams, Zoom, FaceTime, Slack huddles all grab the mic, so this is a universal "user is on a call" signal with no per-app integration. Recompiled as a universal binary (x86_64 + arm64) so it still runs on Intel Macs. **(2) Gated mic poller (main).** New `set-call-watch` IPC: the renderer ARMS the watcher only while music is playing AND the feature is on; main then polls `mic-status` every 3 sec and fires `call-state-changed` on each mic on↔off flip. Gated polling — not an always-on timer — mirrors the 4.4.15 output-device-disconnect watcher. **(3) Per-app output routing (`eq.ts`).** New `setAudioOutputSink()` / `getAudioOutputSink()` wrap `AudioContext.setSinkId()` (Chrome/Electron 110+). Everything JakeTunes plays — music, TTS, stingers — already flows through one shared `AudioContext`, so re-pointing its sink moves *only JakeTunes' audio*. The macOS system default output is never touched, so the Teams call keeps using whatever the OS has it set to. **(4) The route-on-call effect (`App.tsx`).** When a call starts: resolve the configured speaker by NAME against `enumerateDevices()` (Web Audio device IDs churn across sessions, names don't), remember where music was, route it to the call speaker, show an LCD-pill notice ("On a call — music moved to {speaker}"). When the call ends: route back, notice again. Mid-route disable/unmount restores the sink so music is never left stranded on the call speaker. **(5) Preferences → Audio.** New Settings tab: a "Move music to another speaker during calls" toggle and a speaker picker (populated from `list-audio-devices`, AirPlay/Bluetooth/USB transport hints shown inline, built-in output filtered out since routing music TO the speaker the call wants defeats the point, a "(not connected)" fallback row if the saved speaker is currently absent). Help text spells out the AirPlay ~2-sec-latency note so the behavior isn't surprising. **Net effect:** music's playing on the Pebble Pro, you join a Teams call, music hops to your AirPlay speaker on its own and the call has the Pebble to itself — hang up and music comes home. No pausing, no manual device juggling. |
| **4.4.50** | **DJ bubble attribution — the third code path 4.4.49 missed.** Jake: "it still happened. it said the music man was speaking when it was DJ Stephen Hands for DJ mode." 4.4.49 fixed two of the three places the bubble gets populated (the mic button → `'mm'`, the between-track transition handler → `'djhands'`) — but missed the **DJ SET INTRO** path (`startDjSet`, where Stephen introduces the whole rotation: "Built this around disco source code into French house…"). That path calls `setDjText(result.intro)` but never touched `djSpeaker`, so the intro bubble showed whatever the speaker was last set to — i.e. "The Music Man." Fix: `setDjSpeaker('djhands')` at the top of `startDjSet`, alongside the other init calls. All three bubble-populating paths now set the speaker; the misattribution is fully closed. |
| **4.4.49** | **DJ Mode — speech bubble attributes the right speaker + Stephen-decided transition styles (incl. turntable scratch).** Jake: "DJ mode was pressed, and i still see 'the music man is listening'… that dialogue box needs to tie into who is actually doing the fucking speaking. also in DJ mode, i expect real DJ transitions… even some turntable scratches ONLY WHEN APPROPRIATE." **(1) Bubble attribution.** The toolbar speech bubble's label was hardcoded to "The Music Man" — but the bubble shows for *two* speakers: the mic button (Music Man) and DJ Mode between-track commentary (Stephen Hands). So every DJ-Mode line showed up under the wrong name. New `djSpeaker` state, set to `'mm'` in the mic-button path and `'djhands'` at the top of the DJ-transition handler (the bubble only renders in `!radioMode` — i.e. DJ Mode = Stephen's lane). The label and the loading verb both read from it now: "Stephen Hands is on the decks" vs "The Music Man is listening." **(2) Real transitions + scratches.** `musicman-dj`, when Stephen is running a DJ-Mode transition, now also returns a `transition` hint — `talk` / `scratch` / `cut` — that *Stephen himself calls* via a `TRANSITION:` line the handler parses off (and strips from) his spoken text. So "scratches only when appropriate" is the persona's judgment, not a random roll: the prompt tells him scratch is "a spice — rare, earned, never a default," wrong on a mellow/introspective transition, right on a hard genre-or-energy flip. The transition handler branches on it: `cut` slams straight into the next track with no talk (back-to-back bangers), `scratch` punches the seam with a new procedural `scratch` stinger (a bandpass-filtered sawtooth, pitch ramped up-then-down across four accelerating passes, gain hard-gated each pass — the "wikki-wikki," synthesized in Web Audio with zero bundled files, consistent with the existing 4.3.3 stinger system) then plays his line, `talk` is the existing behavior (line in the gap, then the drop). **Scope note:** this is the achievable-now layer of DJ Mode transitions — beatmatched / Camelot-harmonic crossfading (the long-promised "next big feature") still depends on re-enabling the librosa audio-analysis worker and is tracked in `docs/jaketunes-5-plan.md` TIER 3. |
| **4.4.48** | **Persona picks — real variety + a weekly cache that actually holds.** Jake, repeatedly: "there is absolutely no variety in megan nor music man's picks… i feel like the app is not following my rules… it feels like it is resetting a lot." Two distinct bugs, both fixed. **(1) No variety.** The picks prompt has always *said* "each artist at most once" — the model reliably ignores it and returns album-blocks (the screenshot: 4 Talking Heads, 3 Lou Reed, 4 Steely Dan, 4 Bowie in a row). Nothing enforced the rule after generation. New `enforcePicksVariety()` in main does not trust the model: it round-robin-interleaves the model's picks so no artist clusters, caps each artist at **2**, then **backfills from the wider library** — genre-matched, fresh artists, round-robin — when the cap leaves the list short. Last-resort cap-relax only if the library is genuinely artist-thin, so it always returns 25. The prompt also got a loud, boxed `THE #1 RULE — ARTIST VARIETY` block with explicit ✗/✓ examples (all three personas). Distinct *lanes* (MM = canon, Megan = post-punk/alt/critic, Stephen = dance/hip-hop) were already working — this fixes the *within-lane sameness*. **(2) Resetting.** Before 4.4.48 the only weekly cache lived in renderer `localStorage` — per-device, per-component-mount, and bypassed on *any* miss (corrupt entry, fresh install, second device, app-data wipe, navigating away mid-generation). A miss = a fresh Claude call = a totally different list. New main-process `picks-cache.json` in `userData` is the single authoritative weekly cache: once a persona's picks are generated for a given Friday-to-Thursday week, every call that week returns the **exact same list with no Claude hit** — survives restarts, reinstalls, renderer churn. The 3 picks IPC handlers (`musicman-picks` / `megan-picks` / `dj-hands-picks`) are now thin wrappers around a shared `getOrGeneratePicks()` that owns the cache check, the variety pass, and persistence — they can't drift on caching behavior. **Force path:** the Regenerate button now passes `force: true` through a new optional IPC arg, bypassing *both* the renderer cache and main's weekly cache for a true fresh pull. **Stale-cache invalidation:** `PicksData` got a schema stamp (`PICKS_SCHEMA_V = 2`); `loadCachedPicks` rejects entries without it, so the pre-4.4.48 *clustered* localStorage caches get thrown out and regenerated once — through main, which de-clusters them — instead of lingering until Friday. |
| **4.4.47** | **App palette matched to the new logo — one canonical brand orange + off-white, sampled from the icon.** Jake shipped a new JakeTunes app logo and asked: "make sure all orange in the app matches the orange here. same with the off white in that logo." Sampled the flat icon (`newjaketunes.png`, 1254×1254) pixel-precise: the icon orange averages **`#bb4308`** (rgb 187, 67, 8 — a deep burnt terracotta) over 338k pixels; the note's off-white averages **`#faeddc`** (rgb 250, 237, 220) over 20k. The app's old orange was `#d77b27` — noticeably brighter and more amber — so this is a real palette shift, not a find-and-replace. **Brand palette as CSS variables** (`variables.css`): `--brand-orange` (#bb4308, canonical), `--brand-orange-rgb` (for `rgba()` glows/tints), `--brand-orange-light` / `--brand-orange-dark` / `--brand-orange-border` (gradient + border shades derived from the canonical so every glossy iTunes-8 button reads as the logo's hue), `--brand-cream` (#faeddc) + `--brand-cream-rgb`. `--musicman-accent` is repointed to `var(--brand-orange)` — that single line updates ~35 uses across `musicman.css` instantly. **Swept every orange literal** across `app.css` (splash EQ bars, progress shimmer, greeting, bg-glow), `artists.css` (Play Album button, now-playing row accent, expanded-card backdrop), `home.css` (Featured Album button, weather icon, section labels, tour-date stamp, header cream backdrop), `toolbar.css` (DJ-mode + mic buttons, all their glow/border rgba forms — `#e0812e`, `#e88a35`, `#c87828`, `rgba(224,129,46,…)`, `rgba(200,120,40,…)`), `sidebar.css` (highlighted-item icon, the album-art DJ button + its active/hover gradients), `musicman.css` (the `rgba(200,120,40,…)` warm borders ×48, plus `#e67e22` / `rgba(230,126,34,…)` flag colors), `cynthia.css` (input focus ring, typing dots, fix-row outline), and the inline SVG-icon hex in `Sidebar.tsx` (`ICON_HOME_ORANGE`, the Music Man sidebar icon) + `MusicManView.tsx` (rec-card placeholder). Button gradients are now `--brand-orange-light → --brand-orange-dark` with `filter: brightness(1.12)` on hover (replaces the parallel hand-tuned hover gradients, so there's nothing to drift). **Off-white:** the warm header backdrop (`rgba(255,232,196,…)` → `--brand-cream-rgb`) and the white text on the orange "Play Album" buttons (→ `--brand-cream`) — the exact logo pairing (cream note on the orange icon). **Deliberately left alone:** Cynthia's two signature orange→pink multi-stop gradients (`#ff8c42 → #d6336c` and `#ffbd5c → #ff8585 → #e94e7e`) — recoloring one stop of an artistic gradient muddies it; her flat accent uses (focus ring, typing dots) DID move to the brand orange. Also untouched: neutral album-art placeholder grays (`#e8e6e0` etc. — not brand color), the iTunes-8 LCD pill cream (`--pill-top`/`--pill-bottom` — a deliberate period-reference tone), and the iTunes-8 chrome/sidebar grays. Does NOT touch the app icon file itself (`build/icon.icns`) — that's a separate iconset-regeneration task; offer stands. |
| **4.4.46** | **Default smart playlists now sync to the iPod — as real playlists, accurately.** Jake: "please make sure the default playlists (recently added, recently played, top 25 etc) are making it onto the ipod as playlists!! and accurately." Investigation found three linked bugs, all renderer-side (the Python iTunesDB writer `write_itunesdb` was fine — it writes every playlist it's handed as a proper `mhyp` record; it was being handed the wrong set). **(1) Auto-sync omitted the smart playlists entirely.** There are two sync-to-iPod paths: the Device view's manual "Sync" button, which called a local `refreshSmartPlaylists`, and the auto-sync on iPod-connect in `App.tsx`, which passed only `lib.playlists` (the user's regular playlists, filtered to non-iPod). So plugging the iPod in and letting it auto-sync silently shipped zero built-in smart playlists. **(2) The two evaluators had drifted — the "not accurate" part.** `SmartPlaylistView` (what the app DISPLAYS) and `DeviceView`'s `refreshSmartPlaylists` (what SYNCS) were divergent copies: Recently Added was top-50 in the view but top-100 for sync; My Top Rated was 50 vs 25; and Recently Played was *skipped entirely* for sync with the comment "we don't have reliable cross-session data for it." Classic twin-function drift (CLAUDE.md's twin-discovery rule). **(3) Recently Played used non-persistent data.** The display path read `PlaybackState.recentlyPlayed` — an in-memory session list that resets on every app restart, which is *why* it could never sync meaningfully. **Fix:** new `src/renderer/utils/smartPlaylists.ts` is now THE single source of truth — `evaluateSmartPlaylist(id, tracks)` is the one definition, with selection limits in one place. `SmartPlaylistView` calls it for display; the new `buildSmartPlaylistsForSync(tracks, existingPlaylists)` calls it to build the `Playlist[]` for sync — used by BOTH the Device-view button AND the `App.tsx` auto-sync, so they can't drift again and auto-sync now ships the full set. `buildSmartPlaylistsForSync` returns the user's regular playlists too (kept as-is) because the iTunesDB writer treats its input as THE complete playlist set — omitting them would drop them from the device. Recently Played is now backed by the persistent `Track.lastPlayedAt` (epoch ms written on every natural completion in `useAudio`'s `runNaturalEndRef`) — survives restarts, syncs cleanly, and means the app's Recently Played view is now persistent too (a strict improvement). Unified limits: Recently Added 50, Recently Played 50, Top 25 Most Played 25, My Top Rated 50 (with the play-count+recency proxy fallback kept identical on both sides). Built-in smart playlists are emitted even when empty so the playlist EXISTS on the device — an empty "Recently Played" is honest; a missing one was the bug. AI Picks (musicman/megan/dj-hands) are deliberately out of scope — they need an async fetch and aren't part of the iPod-sync set. |
| **4.4.45** | **"Honest queue" — dragging a track into an idle queue now actually plays it.** Jake: "fix queue, it is not honest. if i drag something into queue, it does not play!" Correct — and the root cause is structural. `ADD_TO_QUEUE` and `INSERT_IN_QUEUE` in `PlaybackContext` are pure queue-array mutations: they append/splice into `state.queue` and **never touch `nowPlaying`, `isPlaying`, or `queueIndex`**. Separately, the audio engine in `useAudio.ts` is *imperative*, not reactive — there is no `useEffect` watching `state.nowPlaying`; sound is only ever produced by an explicit `playTrack()` / `loadAndPlay()` call (this is deliberate — crossfade, gapless preload, and DJ Mode all need precise imperative control over the audio graph). Net result: drag a track into the queue while nothing is playing → it lands in the "Up Next" list and just sits there. The UI shows it; playback never starts; the user has to manually double-click it. The queue *looked* like it would play and didn't — "dishonest." **Fix** (in `QueuePanel.tsx`'s `handleDrop`): when the player is idle, instead of dispatching a queue-mutation action, build the would-be new queue locally (splice the dropped tracks into the right slot) and hand it straight to `playTrack(tracks[0], newQueue, startIndex)` — which does the `dispatch(PLAY_TRACK)` + `loadAndPlay()` in one call, the only path that actually produces sound. "Idle" is defined as `!state.nowPlaying` — the unambiguous "player has nothing loaded" state. This covers fresh launch, a cleared queue, and a queue that finished naturally (the natural-end handler dispatches `STOP`, which nulls `nowPlaying`). It deliberately does NOT fire when a track is actively playing OR paused — both keep `nowPlaying` set — so a drag never interrupts a track in progress; in those states it queues normally via the existing `ADD_TO_QUEUE` / `INSERT_IN_QUEUE` path. **Known related (not in this ship):** the context-menu "Add to Up Next" / "Play Next" actions have the same structural dishonesty when idle — they dispatch the same pure-mutation reducer actions. Fixing those is a wider change across the per-view context menus; deferred as a follow-up since Jake's report was drag-specific. |
| **4.4.44** | **Import double-enqueue root cause fixed — session-handled-paths guard + accurate dupe reasons.** Jake, looking at a queue with "source no longer present" on FLAC tracks that DID land in the library: "so good that it recognizes that sexy to someone is in my library but why does it say the source is no longer present for the others even though they ended up in my library anyways?" 4.4.42 treated the symptom (relabeled ENOENT as a soft skip); this fixes the cause. **Root cause:** the same source path gets enqueued twice across two batches — e.g. the chokidar inbox watcher re-fires `add` after a reconfigure, or the user hits "Clear finished" mid-import which removes the original queue items. `enqueueFiles`'s dedup only checked `inQueue` (paths CURRENTLY in `state.items`), so once the first batch's items were processed-and-cleared, a re-enqueue of those same paths sailed straight through. The worker then imported batch 2's copies, found the sources already deleted by batch 1's `deleteInboxSource`, and hit ENOENT — even though batch 1 had imported them perfectly. **Fix: `sessionHandledPaths`** — a module-level `Set<string>` recording every srcPath the worker finishes with (on `done`, on `in-library` dupe, on `source-gone` ENOENT — but deliberately NOT on genuine `failed`, so Retry still works). It's never pruned: survives `clearFinished` / `clearAll` / item removal, resets only on app restart. Three places now consult it: (a) `enqueueFiles` filters incoming paths against it (a re-enqueue of an already-handled album is silently dropped — never becomes a phantom queue row); (b) `enqueueFiles` also now dedups *within* the batch (`importResolvePaths` can return the same file twice if a folder + its contents are both dropped); (c) the worker re-checks it before each `importTrack` call as defense-in-depth, for the case where batch 2 was enqueued before batch 1 finished recording. **Accurate dupe copy:** new `DupeReason` type (`'in-library' | 'already-imported' | 'source-gone'`) drives per-item panel text instead of one catch-all. `in-library` → "already in library" (main-side fingerprint match — what Jake correctly liked for "Sexy to Someone"). `already-imported` → "already imported" (re-enqueue caught by the session guard — the track IS in the library, from the first pass; a clean no-op, not an error). `source-gone` → "source no longer present" (genuine ENOENT with no session record — should be rare now). All three are gray soft-skips with no Retry button; real failures still go red. |
| **4.4.43** | **Now-playing pill centering — symmetric padding after the 4.4.41 visualizer removal.** Jake screenshot: title + scrubber visibly shifted left of center inside the LCD pill. 4.4.41 removed the `MiniVisualizer` rAF rendering but left the pill's CSS padding asymmetric at `4px 56px 4px 16px` — the 56px right padding existed solely to reserve clearance for the now-absent 36px-wide visualizer canvas. Result: centered content (title row + scrubber row) was being centered inside a viewport that extended 40px further on the right than the left, visibly tugging everything leftward. Fix: padding → `4px 22px` (symmetric). 22px clears the `.np-cycle-btn` (positioned at `left:8px width:14px`, ending at x=22) when it appears in activity modes (CD rip / iPod sync); same value on the right keeps centering exact. Trivial CSS-only edit, no JS / DOM changes. |
| **4.4.42** | **Import queue: ENOENT no longer red-flags successful imports + dock out of the floating popup into a docked grid row.** Two Jake-reports from a screenshot showing 9 red `Error: ENOENT: no such fil…` rows on tracks that had actually imported correctly: "these imported well but it still showed as error. also can we put imports in a separate area. its annoying that it is a pop up window." **(1) ENOENT → soft skip, not red error.** Almost every "imported well but shows error" case traces back to a race between the chokidar inbox watcher and a drag-drop hitting the same paths, or from a previous successful run that already removed the source via `deleteInboxSource`. The track is already in the library so a red error with a Retry button (that will never work — the source file is gone) is misleading. Worker now distinguishes `ENOENT` from real failures: if the error message matches `/ENOENT/i`, the queue item is marked `dupe` with an empty `matchedTitle` (sentinel), the ImportQueuePanel renders it as a gray "source no longer present" soft-skip instead of a red Retry-able failure. Real failures (permission errors, conversion failures, etc.) still surface as red with Retry buttons. **(2) Floating overlay → docked grid row.** The 4.4.13-era `ImportQueuePanel` was a `position: fixed` bottom-right dock with a drop shadow, which Jake correctly called a "pop up window" — it overlaid main content and felt modal. Now part of the `.app-shell` CSS grid as a new `imports` row between `content` and `statusbar`. Row has `auto` sizing so when the panel returns null (queue empty), the row collapses to 0 height with no layout cost; when there are items, it grows to the panel's intrinsic size (32px collapsed, 320px max expanded). `.iq-dock` lost its `position: fixed`, `width: 360px`, `border-radius`, and box-shadow — became a full-width chrome strip with a `border-top` divider that visually reads as part of the chrome above the status bar. Slide-in animation on first mount so a fresh batch lands deliberately instead of popping into place. Same buttons (Retry / Clear finished / Close), same per-item rows, same auto-expand-on-failure behavior — only the framing changed. |
| **4.4.41** | **Music Man knows the difference between "unheard" and "skipped"; pill visualizer removed (battery + cycles).** Two Jake-asks. **(1) Visualizer removed.** "get rid of the visualizer on the far right it is useless and kills battery." The `MiniVisualizer` rAF loop in `NowPlaying.tsx` computed RMS amplitude from the audio waveform every frame (~60 fps continuously while playing) and re-painted a 60×8 px squiggle — kept the renderer's compositor and the audio-thread sample loop busy for a UI element that conveyed essentially zero useful information. JSX call removed; component definition kept in the file so a future revival doesn't have to recreate it from scratch. Net: one fewer rAF callback per frame on the renderer thread + zero canvas repaints per second during playback. **(2) Music Man + skipCount.** "music man should know that if i have no plays on a song....that doesnt mean i didnt skip it." The renderer-side library context that gets fed to Music Man (built in `App.tsx` after `loadTracks` resolves) was previously just `${count} total tracks. Top artists: …. Top genres: ….` — entirely playCount-blind, and entirely skipCount-blind. Music Man couldn't tell the difference between "Jake has never heard this" and "Jake has skipped this song every single time it's come on." Both showed `playCount: 0`. New context now exposes five dimensions: top artists by track count (catalog signal), top artists by total playCount (engagement signal), top artists by total skipCount (rejection signal), HEARD-BUT-SKIPPED artists (`skipCount > 0 AND playCount == 0` — "Jake's heard them; he just doesn't want them"), and ACTIVE-DISLIKE specific tracks (`skipCount ≥ 3 AND playCount == 0` — a hard "don't recommend"). Plus an explicit reasoning note at the bottom: *"A track with playCount == 0 is NOT necessarily unfamiliar to the user. Always check skipCount first. If a track or artist appears in the 'Heard-but-skipped' or 'actively-rejected' lists above, the user has heard it and chose to skip — do not surface it as a 'discovery' or 'you should try this.' The true preference signal is roughly (playCount − 0.5 × skipCount), not playCount alone."* Also enhanced `buildTasteProfile()` in main (used by Picks generation): added a `Recently skipped tracks` list (specific track titles deduped from `listenerProfile.recentSkips`, up to 10) and the same reasoning rule at the end. Net: Music Man chat, Picks, and observations all now route around tracks the user has actively skipped instead of pitching them as fresh recommendations. |
| **4.4.40** | **Artists view de-awkwarded — stacked layout, real artist photos, now-playing pill time-stuck fix.** Jake on the 4.4.38 polish: "artist page very awkward." Screenshot showed J. Cole's *Born Sinner* / Descendents' *Milo Goes to College* expanded with the cover on the left at 180px tall and a 15-track tracklist on the right that extended way past the cover — leaving a huge cream-gradient void below the cover. Plus a tiny "Cool To Be You" collapsed card sandwiched between two expanded ones, which read as visual whiplash. Three real fixes plus the now-playing pill bug. **(1) Expanded-card layout: grid with named areas.** The 4.4.37/38 design was `flex: [cover 180px] | [info+tracklist 1fr]`. Switched to `display: grid; grid-template-areas: "art info" / "tracks tracks"`. Cover (now 220×220px, up from 180) sits in the top-left area. Title + meta strip + Play Album button sit in the top-right info area, vertically centered against the cover so there's no empty band below the Play button. Tracklist sits in its own row spanning BOTH columns underneath — so it never creates a "longer-than-cover" mismatch. Subtle top border separates header from tracklist. **(2) Tall albums auto-split into 2 columns.** New `.artist-album-card--tall` modifier (applied when `tracks.length > 10`) sets `column-count: 2; column-gap: 32px; column-rule: 1px solid` on the tracklist. Each `.artist-track-row` gets `break-inside: avoid` so a row never gets split mid-column. Means a 15-track Descendents album now reads as 8 + 7 across two columns instead of a 15-row tower. Each row still uses its own internal `display: grid` for `[icon] [num] [title] [time]`, fully compatible with the parent's column flow. **(3) Real artist photos via Bandsintown.** New `getArtistImage(artist)` in `src/main/index.ts` hits Bandsintown's `/artists/{name}?app_id=jaketunes-desktop` endpoint (same auth as the 4.4.32 tour-dates fetch — no key needed), downloads `image_url`, saves to `userData/artist-images/{slug}.jpg`. Slug is filename-safe `[a-z0-9-]+` (no traversal possible). 30-day disk-cache hit-and-miss tombstones (a `.miss` file when Bandsintown 404s, so repeat probes don't hit the network for artists they don't index). Single-flight `Map<slug, Promise>` collapses concurrent calls. New `artist-image://` privileged scheme + protocol handler mirror the 4.0 `album-art://` pattern. ArtistsView prefetches photos for all visible artists on mount, throttled 6-concurrent, and renders an `<img>` in place of the colored-initials circle when a slug comes back. Avatar bumped 28px → 38px so real photos read clearly and the fallback feels less cramped. Skips Bandsintown placeholder image URLs and <200-byte responses. **(4) Now-playing pill time-stuck-at-0:00 bug.** Jake's screenshot of the LCD pill: "Wait — Earshot — Two" with `0:00` / `-0:00` even though the song was actively playing through the speakers. Root cause: `useAudio.ts:347-350` only dispatched `SET_POSITION` / `SET_DURATION` when Howler's `sharedHowl.playing()` returned true. Howler can return false even when the underlying HTMLAudioElement is actively producing sound — happens after the 4.4.9 pool-reuse rebind path, or after Core Audio renegotiates the output device (4.4.15 territory). Position froze at the React reducer's initial `0` and the pill never updated. Fix: added a third tier to the `positionHowl` selector that falls through to `sharedHowl` when `stateRef.current.isPlaying` is true even if `sharedHowl.playing()` lies. Howler's `seek()` reads the underlying element directly so the true position dispatches even when `.playing()` is wrong. Crossfade behavior preserved: the outgoing-howl tier still wins when both are actively playing. **(5) Animation polish.** Expanded card now eases in over 360ms (was 320) with a small `translateY(-4)` so it "lands" instead of just appearing. Tracklist slides in 80ms after the card so the cover lands first — feels more deliberate. Artist-row chevron rotation now uses cubic-bezier(0.2, 0.65, 0.3, 1) for 200ms (was linear 150ms). Avatar in-anim (`artist-avatar-in`) when a photo finishes loading. JSX restructure: `.artist-album-tracklist` is now a SIBLING of `.artist-album-info` (not a child) so CSS grid can place it in its own row. |
| **4.4.39** | **Cool app splash screen — welcoming greeting, pulsing EQ bars, shimmering progress bar, tagline.** Jake: "when mac os app is opening, we need a loading message....and make it cool. progress bar and all. welcoming the user to the greatest platform ever." Replaces the previous tiny gray "JakeTunes / Loading library..." placeholder. New `src/renderer/components/SplashScreen.tsx` + rewritten `.app-splash*` block in `app.css`. **Visual:** Dark navy radial-gradient backdrop with two slow-drifting warm/cool color glows (9-sec sine-wave drift, alternate) — the warm-orange glow lives bottom-right, cool-blue glow upper-left, both well below content contrast so they read as ambience, not noise. Same color palette as the Home Featured Album hero (4.4.33) and ArtistsView expanded card (4.4.38) so the splash feels like the same app. **Components, top to bottom:** (1) Seven pulsing vertical EQ bars in JakeTunes orange (`linear-gradient(180deg, #f5b074, #d77b27, #a85510)`), each with a staggered `animationDelay` and slightly varying `animationDuration` (0.78s + (i % 3) × 0.14s) so the row moves like real audio levels instead of a synced chorus. Box-shadow glow gives them a slight bloom. (2) Big 48px "JakeTunes" wordmark with `-0.028em` letter-spacing, gradient-clipped from white to soft blue-gray for a metallic feel. (3) Tagline: "The greatest music platform ever built." (4) Time-of-day greeting in warm orange: "Good morning, Jake." / "Good afternoon, Jake." / "Good evening, Jake." / "Burning the midnight oil, Jake." — same convention as the 4.4.29 Home header. (5) Slim 260×4px progress bar — fill animates from 5%→92% over 1.6 sec via rAF with ease-out-cubic; jumps to 100% over 240ms when the parent flips `isReady`. The fill has a shimmer animation (background-position pan) so it visibly "flows" left-to-right while filling — looks like light passing through. (6) Uppercase tracking-out status text below that rotates every 480ms through `["Reading your library…", "Loading playlists…", "Building artwork index…", "Tuning the amps…", "Almost there…"]` and pins to "Ready." once `isReady`. Min-height pinned so text swaps don't cause layout shift. **Min display time gate:** Even on a warm-cache load where App.tsx's Promise.all settles in <500ms, the splash holds for ≥1400ms (new `splashMinElapsed` state in `App.tsx`) so the wordmark/greeting/EQ actually get to land — without this it would strobe on fast hardware. App becomes interactive only when BOTH the data load is done AND the min-time elapses. The `isReady` prop snaps progress to 100 + status to "Ready." during the held tail, so the user sees a satisfying "done" frame before the main UI fades in. **A11y:** `prefers-reduced-motion: reduce` is honored — all animations are cancelled, EQ bars settle to a static `scaleY(0.6)`, layout still looks polished. EQ row is `aria-hidden`. Per-stage real progress (not fake-animated) is out of scope because the load is a single Promise.all in App.tsx — splitting it would be invasive for indistinguishable UX. |
| **4.4.38** | **Artists view expanded album — polish pass. "Awkward" → "cooler."** Jake's 4.4.37 fix made it functional ("better but not as smooth as i want. a little awkward. needs to look cooler"). Six small changes on top of the 4.4.37 grid fix, all targeting the "feels like a real album-detail card" pass. **(1) Open animation.** New `artist-album-expand` keyframe (`opacity 0.5 → 1`, `scale 0.985 → 1`, 320ms `cubic-bezier(0.2, 0.65, 0.3, 1)`) — the card no longer hard-pops into place when toggled. Matches the easing curve used by the 4.4.24 row stagger and view-fade. **(2) Bigger cover, real drop shadow.** Cover bumped 140px → 180px and gets a prominent `box-shadow: 0 8px 24px rgba(0, 0, 0, 0.22)` — same depth treatment the Home Featured Album hero (4.4.33) uses. Hover override deepens the shadow without translating the card (the old `translateY(-2px)` hover fought against the open animation). **(3) Metadata strip.** Between the title and the tracklist there's now `2013 · 13 songs · 45 min · 47 plays` — year (from any track's `year` field), song count, total duration in minutes (sum of `track.duration`), and aggregate play count (sum of `track.playCount`). Tabular nums so the dot-separated parts line up cleanly. Hides gracefully when individual stats aren't available (no plays yet → no plays segment). **(4) Play Album CTA.** Compact orange-gradient button (`linear-gradient(180deg, #d77b27, #b56419)`) with a play triangle, sits below the metadata strip. Same primary-button visual language as the Home Featured Album. `align-self: flex-start` so it hugs the left edge of the info column. Clicks play the whole album top-to-bottom via `PLAY_TRACK` + `SET_QUEUE`. **(5) Bigger, tighter title.** Expanded title goes 600/14px → 700/22px with `letter-spacing: -0.015em` and `line-height: 1.15` — reads as a real album-detail header instead of an oversized list-row label. **(6) Now-playing row accent.** The 4.4.37 "no row fill" rule is replaced with a subtle warm gradient (`linear-gradient(90deg, rgba(215, 123, 39, 0.10), rgba(215, 123, 39, 0.02) 60%)`) plus a 2px inset box-shadow on the left edge (`box-shadow: inset 2px 0 0 #d77b27`) so the playing row gets a visible accent without shifting any of the grid columns (inset, not border). Title bolds to 700 + `color: #b56419`; number and duration shift to a 70%-alpha orange so the whole row reads as "this one is playing" at a glance. Plus a subtle warm-cool gradient backdrop on the expanded card itself (`linear-gradient(135deg, rgba(215, 123, 39, 0.06), rgba(74, 144, 217, 0.04))`) — same color story as the Home Featured Album hero, so the two large-format album displays in the app share a visual family. All CSS, no JS/TS structural changes beyond the metadata-strip + button JSX additions in `ArtistsView.tsx`. |

---

## Architecturally significant pieces (cross-version reference)

### The cast — `src/main/cast.ts`
Single source of truth for the 9-caller rolodex. Each caller has: id, name, tag (e.g. `[GIOVANNI]`), voiceId, weight (selection), fn (function), bg (background), speech (profile), openings (3 examples), mmReaction, meganReaction, never[] (taboos), voiceSettings (stability/similarity_boost/style overrides). `buildCallerSegmentMode(callerId)` returns the per-caller prompt block. `callerForTag(tag)` is the parser lookup.

### Persistent show memory — `src/main/radio-memory.ts`
JSON in `userData/radio-memory.json`. Tracks: last N topic angles, last N callback-worthy lines (extracted by heuristic — strong opinions, profanity, quoted lyrics), `hotTake` (slot-1 Cold Open Hot Take, scoped per-hour, paid off at slot 11), running-bit state. `formatMemoryForPrompt()` is what the radio handler injects.

### Segment archetypes — `src/main/archetypes.ts`
11 structural templates as a data table. `buildArchetypeBlock(id, opts)` returns the prompt fragment. Deferred-Punchline / Hour-Out archetypes consume the slot-1 hot take from memory.

### External APIs — `src/main/external.ts`
6 sources: OpenWeatherMap, Last.fm, RSS (Pitchfork/Stereogum/Quietus), Discogs, Wikidata SPARQL, Cover Art Archive (+ MusicBrainz release MBID lookup). All cached with TTL via `makeCache<T>(ttlMs)`. All fail-soft (return null/empty on error). Format-for-prompt helpers (`formatWeatherForPrompt`, `formatLastFmChartForPrompt`, etc.) return single-line summaries to inject into the radio prompt.

### Audio graph — `src/renderer/audio/eq.ts`
- `audioContext` (shared with Howler via `Howler.ctx`)
- `preampNode` → 10-band biquad filter chain → `audioContext.destination`
- `analyserNode` side-branched off preamp for the LCD-pill visualizer
- `attachHowlToEq(howl)` — music elements (idempotent, handles Howler pool reuse)
- `attachClipToBroadcast(audio)` — TTS clips (route through preamp; clean up on `ended`/`error`)
- `attachAnnouncerToBroadcast(audio)` — announcer voice (goes through broadcast FX chain → preamp; clean up on `ended`/`error`)
- Broadcast FX chain: compressor (8:1, -22dB) → low shelf +4dB @ 180Hz → presence peak +5dB @ 2.8kHz → high shelf +2dB @ 8kHz → split dry/wet (convolver IR = synthesized exp-decay noise, 1.5s tail)
- Recording: `MediaStreamAudioDestinationNode` taps the chain tail + Howler master gain; `MediaRecorder` produces webm/opus; main process transcodes to MP3 via ffmpeg

### Procedural stingers — `src/renderer/audio/stingers.ts`
Web Audio synthesis (no bundled SFX files): riser (saw + LP sweep), swoosh (bandpass-swept noise), drum-hit (sine pitch drop + click), bell-hit (fundamental + perfect 5th sines), sub-drop, whoosh-pad. `randomPreStinger()` / `randomEndStinger()` for the announcer drop production.

### Listener taste profile — `src/main/index.ts` (around `buildTasteProfile`)
`userData/listener-profile.json`: totalPlays, totalSkips, artistPlays, artistSkips, albumPlays, genrePlays, recentPlays[200], recentSkips[100], topRated[50], observations[15]. Auto-observations regenerated every 20 plays via a small Claude call. Fed into every persona prompt (Picks, chat, radio chatter, recommendations).

### Disabled / not yet wired
- **Audio-analysis (librosa) worker**: disabled in 4.2.12. `enqueueAudioAnalysis` is a no-op. Was racing with playback decoder.
- **Stuck-audio watchdog** (4.1.5 implementation): replaced by the heartbeat recovery in 4.2.14.
- **DJ Mode (Camelot mixing)**: the vinyl button is wired and Stephen Hands is the persona, but the actual beatmatched-Camelot transitions are placeholder. Real BPM/key-aware crossfading is the next big feature.

### Rotation slot map (current — 4.4.1 hour clock)
| Slot | Role | Archetype |
|---|---|---|
| 0 | Full station ID + show open | (built into segmentMode) |
| 1 | Cold Open Hot Take (captured to memory) | A |
| 2 | Back-announce | (Back-Announce) |
| 3 | Lateral Pivot or Lineage Bridge (50/50) | B or E |
| 4 | Brooklyn / Lyric Roast / Lightning Round (random) | I / F / C |
| 5 | Caller (full 9-person rolodex) | G |
| 6 | Recovery cool-down | (Recovery) |
| 7 | Mini station ID (dry, short) | (built in) |
| 8 | Historian Dwell — slot 8 ONLY | J |
| 9 | Stephen Hands (1 hour in 3) OR Lightning Round | H or C |
| 10 | Recovery cool-down | (Recovery) |
| 11 | Hour Out (pays off slot-1 hot take) | K |

### Cast voice IDs (current, for reference)
| Character | Voice ID |
|---|---|
| Music Man | `ljX1ZrXuDIIRVcmiVSyR` (env override: `ELEVENLABS_VOICE_ID`) |
| Megan | `T7eLpgAAhoXHlrNajG8v` |
| Announcer | `CeNX9CMwmxDxUF5Q2Inm` |
| DJ Stephen Hands | `ApBE43wHy5MiZGz9ihqB` |
| Giovanni | `UOB3uZCEf2cjGpZaGOXq` |
| Rajiv | `miqykcv8BCUvQnRlIGUV` |
| Bernard | `Q0HZwrR1H2SmRvd5cX3U` |
| LaShonte | `VYtAZPRhkK9OruILpVBz` |
| Kristina | `BlgEcC0TfWpBak7FmvHW` |
| Devin | `YrAYvOVjAFiqVwBgB4qI` |
| Maya | `aKw9UnnjRq5scbeeGI7Z` |
| Mike | `Ib97zM6uFBc71OWgj75I` |
| Zoe | `c8v8wiyiDwyuduufV6kB` |

### Current persona system prompts (where they live)
- `MUSIC_MAN_CORE` — `src/main/index.ts`, around line 3117. Fixed opinions list.
- `MEGAN_CORE` — `src/main/index.ts`, around line 3279. Non-overlapping fixed opinions.
- `DJ_HANDS_CORE` — `src/main/index.ts`, just after `MEGAN_CORE`. Party-first.
- Per-caller prompts — `src/main/cast.ts` via `buildCallerSegmentMode()`.

### Open items in flight (parallel session)
- A `'store'` ViewName + `musicmanStoreReview` IPC + Store icon in sidebar (`ICON_STORE_BLUE`) suggest a JakeTunes Store view is being scaffolded. Not yet documented here — defer to that session.

---

## Quick install reference (any version)

- DMG: `release/JakeTunes-X.Y.Z-arm64.dmg`
- Build: `npm run dist:dmg`
- Type-check: `npx tsc --noEmit`
- User data: `~/Library/Application Support/JakeTunes/`
- Music files (local "iPod" mirror): `~/Music2/JakeTunesLibrary/iPod_Control/Music/` (legacy path; default for new installs is `app.getPath('music')/JakeTunesLibrary/iPod_Control/Music/`)

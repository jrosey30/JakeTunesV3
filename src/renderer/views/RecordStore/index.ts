// Step Inside routes to the playable shop (2026-09-09). The 2D pixel room
// lives on in RecordStoreView.tsx, unrouted, until the game replaces every
// part of it that earned its keep.
export { default } from './StepInsideRoute'

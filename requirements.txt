# Python dependencies for JakeTunes V3.
# Install with: pip install -r requirements.txt
#
# These power the helper scripts in core/ that the Electron main process
# invokes via spawn(PYTHON_CMD, ...).

# Audio file tag I/O (used by tag_reader.py, tag_writer.py, reembed_tags.py,
# alac_compat_fix.py, dedupe_compilation_years.py — basically every script
# that touches embedded metadata).
mutagen

# Audio analysis (4.0 §2.4 — BPM, key, Camelot wheel for DJ-grade
# transitions and harmonic playlists). librosa handles both: tempo via
# beat_track, key via chroma_cqt + Krumhansl-Schmuckler.
#
# (aubio was originally specced for BPM but its source last shipped in
# 2019 and fails to compile against modern numpy on Python 3.14+.
# librosa-only is the simpler architecture regardless.)
librosa

# NAS music engine (scripts/music_engine.py / custom_audio_engine.py) —
# inbox watcher. Optional; without it the engine falls back to a 5s poll loop.
watchdog

# HTTP client for custom_audio_engine.py (AudD upload + Deezer/MB/Discogs).
requests

# AI sampler engine (scripts/ai_sampler_engine.py) — optional DSP stack.
# librosa (above) already covers transient / onset chopping.
# soundfile speeds stem I/O; demucs is the preferred local stem separator
# (spleeter works as a fallback if installed). Without demucs/spleeter the
# engine can still chop+sequence a full mix via AI_SAMPLER_SEPARATOR=none.
soundfile
# demucs   # pip install demucs  (torch; large — install on the NAS/homemini host)
# spleeter # legacy alternative; prefer demucs
